[package]
name = "yes-you-can-core"
version = "0.1.0"
edition = "2021"
authors = ["CAZE7 <info@arena.ai>"]
description = "Experimental Classic-CAN ISO-TP / signal / safety sketch — not wired, not in CI"
license = "MIT"

[dependencies]

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
