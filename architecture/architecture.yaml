{
  "schemaVersion": 2,
  "description": "The one source of the architecture rule and the AI context layer (AGENTS 28, master backlog P0 #2, ADR 0031/0042/0043). Read by tools/architecture/check-dependencies.mjs (fails the build on a forbidden edge), tools/architecture/ai-context.mjs (generates the .ai/generated/<topic>-context.md bundles), tests/architecture (keeps both tools honest). Nothing else may restate these edges or these layer assignments — a second copy is a second rule. Format: this file is written in JSON syntax, which is valid YAML 1.2, so it parses with Node's built-in JSON.parse without any runtime dependency (ADR 0002) and with any YAML reader at the same time. If you need YAML comments or flow style, you need a parser, and a parser is a dependency.",
  "layers": {
    "foundation": "Shared primitives and the DOM-free chart core. Nothing may sit below this layer; these packages import nothing.",
    "contract": "Pure data contracts and projections (domain model, diagnostic IR, definitions). No I/O, no protocol, no transport, no node builtins — that is what makes them the stable middle.",
    "application": "Commands, queries and actions: the vocabulary of what a client may ask the platform to do. Speaks domain only.",
    "protocol": "Diagnostic protocol logic (UDS ISO 14229, KWP2000, OEM hooks) that speaks through a link seam, never through a bus or adapter.",
    "transport": "Frame layer (CAN), segmentation (ISO-TP ISO 15765-2) and the DoIP link (ISO 13400). Below protocols, above adapters.",
    "adapter": "Hardware and interface adapters that implement the CanBus contract. Hardware specifics stay here and nowhere else.",
    "core": "The diagnostic engine: session, discovery, DTC scan/track, measurement, write port. The widest package below runtime.",
    "runtime": "The composition root: domain services, command/query handlers, domain events, audit. Headless — no HTTP, DOM or UI toolkit.",
    "persistence": "Session persistence, versioned migrations and export formats. The only layer below the app that touches files.",
    "presentation": "Charts and reports: rendering what the core produced. They read the diagnostic IR and domain views; they never reach a vehicle.",
    "ai": "Analysis providers: they read the diagnostic IR (evidence set, hypotheses) and nothing else. An analysis proposes; the write chain decides.",
    "ui": "The workbench application. The app depends on the layers, never the other way round.",
    "tool": "Development and QA tools (simulators, golden sessions, trace analyzer, definition importer). A tool, not a layer: nothing imports them."
  },
  "packages": {
    "@vdp/shared": {
      "layer": "foundation",
      "mayImport": [],
      "why": "Foundation: no dependencies at all."
    },
    "@vdp/charts": {
      "layer": "foundation",
      "mayImport": [],
      "why": "DOM-free chart core; knows its own types and nothing else."
    },
    "@vdp/protocols-oem": {
      "layer": "protocol",
      "mayImport": [],
      "why": "Manufacturer hook types and registry; fills gaps, depends on nothing."
    },
    "@vdp/domain": {
      "layer": "contract",
      "mayImport": ["@vdp/shared"],
      "why": "Domain contracts: shared primitives only — never protocols, transports or I/O."
    },
    "@vdp/diagnostic-ir": {
      "layer": "contract",
      "mayImport": ["@vdp/shared"],
      "why": "The diagnostic IR: observations with provenance between raw protocol form and projections. It knows no protocol, no transport and no I/O — that is what makes it the stable middle (P0 #6)."
    },
    "@vdp/application": {
      "layer": "application",
      "mayImport": ["@vdp/domain"],
      "why": "Application layer: commands/queries/actions speak domain only."
    },
    "@vdp/definitions": {
      "layer": "contract",
      "mayImport": ["@vdp/shared"],
      "why": "Definitions are data + validation; no diagnostic logic."
    },
    "@vdp/transport-can": {
      "layer": "transport",
      "mayImport": ["@vdp/shared"],
      "why": "Frame layer; nothing below it."
    },
    "@vdp/transport-iso-tp": {
      "layer": "transport",
      "mayImport": ["@vdp/shared", "@vdp/transport-can"],
      "why": "Segmentation sits on frames."
    },
    "@vdp/transport-doip": {
      "layer": "transport",
      "mayImport": ["@vdp/shared", "@vdp/transport-can"],
      "why": "DoIP presents itself as a CAN-like link; it never pulls in the protocol layer."
    },
    "@vdp/protocols-uds": {
      "layer": "protocol",
      "mayImport": ["@vdp/shared"],
      "why": "Speaks through a link interface, never through a bus or adapter."
    },
    "@vdp/protocols-kwp2000": {
      "layer": "protocol",
      "mayImport": ["@vdp/shared", "@vdp/protocols-uds"],
      "why": "Legacy ECUs share the UDS primitives."
    },
    "@vdp/adapter-generic-can": {
      "layer": "adapter",
      "mayImport": ["@vdp/shared", "@vdp/transport-can"],
      "why": "Adapter implements the bus contract; hardware specifics stay here."
    },
    "@vdp/adapter-elm327": {
      "layer": "adapter",
      "mayImport": ["@vdp/shared", "@vdp/transport-can"],
      "why": "Serial ELM327 protocol and stream belong to the adapter."
    },
    "@vdp/adapter-canable": {
      "layer": "adapter",
      "mayImport": ["@vdp/shared", "@vdp/transport-can", "@vdp/adapter-elm327"],
      "why": "CANable reuses the ELM327 stream/protocol."
    },
    "@vdp/adapter-socketcan": {
      "layer": "adapter",
      "mayImport": ["@vdp/shared", "@vdp/transport-can"],
      "why": "Native SocketCAN binding."
    },
    "@vdp/adapter-host": {
      "layer": "adapter",
      "mayImport": [
        "@vdp/shared",
        "@vdp/transport-can",
        "@vdp/adapter-canable",
        "@vdp/adapter-elm327",
        "@vdp/adapter-socketcan"
      ],
      "why": "Host bindings (tty, bindings loader) and the side-effect-free probe catalog."
    },
    "@vdp/core": {
      "layer": "core",
      "mayImport": [
        "@vdp/shared",
        "@vdp/diagnostic-ir",
        "@vdp/definitions",
        "@vdp/protocols-uds",
        "@vdp/protocols-oem",
        "@vdp/transport-can",
        "@vdp/transport-iso-tp"
      ],
      "why": "The diagnostic core: protocols + transports + definitions (still the widest package below runtime)."
    },
    "@vdp/runtime": {
      "layer": "runtime",
      "mayImport": [
        "@vdp/shared",
        "@vdp/diagnostic-ir",
        "@vdp/domain",
        "@vdp/application",
        "@vdp/core",
        "@vdp/definitions",
        "@vdp/protocols-uds",
        "@vdp/transport-can",
        "@vdp/transport-doip"
      ],
      "why": "Composition root: wires the transport seam, so it may reach the concrete transports while domain/application stay free of them."
    },
    "@vdp/storage": {
      "layer": "persistence",
      "mayImport": ["@vdp/shared", "@vdp/core"],
      "why": "Persistence and export formats below the runtime."
    },
    "@vdp/reports": {
      "layer": "presentation",
      "mayImport": ["@vdp/core", "@vdp/diagnostic-ir"],
      "why": "Report rendering prints what the core produced — and reads the diagnostic IR directly, because a section that shows an unproven statement has to use the IR's own evidence words instead of restating them (P0 #6, ADR 0037)."
    },
    "@vdp/ai": {
      "layer": "ai",
      "mayImport": ["@vdp/shared", "@vdp/diagnostic-ir"],
      "why": "Provider abstraction stays orthogonal to diagnostics: it may read observations and their evidence — the diagnostic IR, which names no transport, no protocol and no write — and nothing above it. That is the machine-checked form of „an analysis provider never touches the vehicle“ (AGENTS 22, P0 #16/#39, ADR 0038)."
    },
    "@vdp/simulators": {
      "layer": "tool",
      "mayImport": [
        "@vdp/shared",
        "@vdp/core",
        "@vdp/definitions",
        "@vdp/protocols-uds",
        "@vdp/transport-can",
        "@vdp/transport-iso-tp"
      ],
      "why": "Virtual vehicle answers like a real ECU stack."
    },
    "@vdp/golden-sessions": {
      "layer": "tool",
      "mayImport": [
        "@vdp/shared",
        "@vdp/diagnostic-ir",
        "@vdp/definitions",
        "@vdp/transport-can",
        "@vdp/transport-iso-tp",
        "@vdp/core",
        "@vdp/simulators"
      ],
      "why": "Recorded sessions plus their expectation: it records against the simulator, replays through the real core and compares in the IR vocabulary. A tool, not a layer — nothing imports it."
    },
    "@vdp/trace-analyzer": {
      "layer": "tool",
      "mayImport": ["@vdp/shared", "@vdp/definitions", "@vdp/protocols-uds", "@vdp/transport-can"],
      "why": "Offline trace tooling decodes with the protocol layer, without the core."
    },
    "@vdp/harvest": {
      "layer": "tool",
      "mayImport": [
        "@vdp/shared",
        "@vdp/definitions",
        "@vdp/protocols-uds",
        "@vdp/transport-can",
        "@vdp/transport-iso-tp",
        "@vdp/core",
        "@vdp/storage",
        "@vdp/adapter-host",
        "@vdp/simulators"
      ],
      "why": "Read-only vehicle harvest: it drives discovery, the UDS read services and the fault memory through core and the transports, and writes its result as an observation, an ODX/PDX description and a definition candidate (ADR 0058). A tool, not a layer — nothing imports it. It may reach core and the transports because its whole job is to drive them, it reaches storage only for the ZIP container of a PDX, adapter-host for the CLI's bus selection, and simulators so a harvest can be run and tested without hardware (ADR 0005)."
    },
    "@vdp/definition-importer": {
      "layer": "tool",
      "mayImport": ["@vdp/shared", "@vdp/definitions"],
      "why": "Imports definition data; validation is the definitions package."
    },
    "@vdp/web": {
      "layer": "ui",
      "mayImport": [
        "@vdp/shared",
        "@vdp/domain",
        "@vdp/application",
        "@vdp/runtime",
        "@vdp/diagnostic-ir",
        "@vdp/definitions",
        "@vdp/protocols-uds",
        "@vdp/transport-can",
        "@vdp/storage",
        "@vdp/reports",
        "@vdp/ai",
        "@vdp/simulators",
        "@vdp/adapter-host"
      ],
      "why": "The workbench speaks to the vehicle exclusively through the runtime (command/query bus). It reads the diagnostic IR to hand the evidence set to an analysis provider: restating evidence vocabulary in the app would put a second copy of the claim's source where the IR already says it (P0 #6/#39, ADR 0037/0038)."
    },
    "@vdp/formal-conformance": {
      "layer": "tool",
      "mayImport": ["@vdp/shared", "@vdp/core", "@vdp/transport-can", "@vdp/transport-iso-tp"],
      "why": "Runs the shared test vectors against the production TypeScript (ISO-TP transport, write-safety chain) and compares both sides with the Haskell reference. A tool, not a layer — nothing imports it; it may reach core and the transports because its whole job is to drive them. (ADR 0045)"
    }
  },
  "rules": {
    "nodeBuiltins": {
      "allowedIn": ["@vdp/adapter-host", "@vdp/storage", "@vdp/web", "@vdp/golden-sessions", "@vdp/formal-conformance", "@vdp/harvest"],
      "why": "Portable layers (domain, application, protocols, transports, core, runtime) must stay free of node:fs and sockets; file system and host bindings belong to storage, host adapters and apps. @vdp/golden-sessions is a tool, not a layer — nothing imports it, and its CLI is the part that reads and writes the fixture files on disk. @vdp/formal-conformance is a tool whose CLI reads the vector files and runs the Haskell toolchain — the same file-and-process boundary golden-sessions owns, for the same reason (ADR 0045). @vdp/harvest is a tool whose CLI writes the observation, the ODX description and the PDX container to disk and spawns the optional odxtools cross-check — the same boundary again (ADR 0058)."
    },
    "ui": {
      "package": "@vdp/web",
      "why": "No package imports the UI — the app depends on the layers, never the other way round."
    },
    "layerRules": [
      {
        "from": "@vdp/protocols",
        "forbidden": ["@vdp/adapter"],
        "why": "Protocols speak through a link, never through an adapter."
      },
      {
        "from": "@vdp/transport",
        "forbidden": ["@vdp/protocols"],
        "why": "Transports are the frame layer; protocols sit above them."
      },
      {
        "from": "@vdp/adapter",
        "forbidden": ["@vdp/protocols", "@vdp/core"],
        "why": "Hardware specifics stay below protocols and the core."
      }
    ],
    "portableLayers": [
      {
        "packages": ["@vdp/domain", "@vdp/application"],
        "forbidden": [
          "@vdp/protocols",
          "@vdp/transport",
          "@vdp/adapter",
          "@vdp/core",
          "@vdp/runtime",
          "@vdp/storage"
        ],
        "why": "Domain and application stay protocol-, transport- and I/O-free (AGENTS 1, 3)."
      }
    ]
  },
  "contracts": {
    "@vdp/ai": {
      "why": "The analysis seam: AnalysisProvider, AnalysisInput and the provenance every answer has to carry. A closed or third-party provider implements exactly this surface and nothing else — an analysis proposes, the write chain decides (ADR 0038/0059)."
    },
    "@vdp/core": {
      "entry": "dist/src/logging/integrity.d.ts",
      "why": "Not the engine — the integrity seam: IntegrityPort, the canonical raw-trace binding and the manifest an export carries (ADR 0047/0057). A storage layer, a cloud worker or a hardware module implements the hashing side of this module, so this module is the contract."
    },
    "@vdp/definitions": {
      "why": "The data contract: schema versions, mandatory provenance and the validator every definition package must satisfy, no matter who wrote it or who licensed it (ADR 0003/0023/0024)."
    },
    "@vdp/diagnostic-ir": {
      "why": "The intermediate representation between a measurement and a conclusion. Anything that reads evidence and proposes a hypothesis — open, closed or a customer integration — consumes and produces exactly these types (ADR 0034/0038)."
    },
    "@vdp/domain": {
      "why": "The ports and the capability vocabulary: the only language in which a consumer may ask the platform for something (ADR 0014/0059)."
    },
    "@vdp/protocols-oem": {
      "why": "The OEM hook contract (OemProtocol, the registry, the provenance a hook must name), with no imports of its own — the template a closed manufacturer module fills in (ADR 0003/0059)."
    },
    "@vdp/protocols-uds": {
      "entry": "dist/src/security.d.ts",
      "why": "Not the UDS client — the seed&key seam: SeedKeyAlgorithm and the refusal default. A workshop registers an algorithm it is entitled to use; that registration is the contract, and the default that refuses is part of it (AGENTS 26/34.12)."
    }
  },
  "licenses": {
    "production": {
      "allowed": [
        "MIT",
        "Apache-2.0",
        "BSD-2-Clause",
        "BSD-3-Clause",
        "ISC",
        "0BSD",
        "Unlicense",
        "CC0-1.0"
      ],
      "forbidden": [
        "GPL",
        "AGPL",
        "LGPL",
        "MPL",
        "EPL",
        "CDDL",
        "SSPL",
        "BUSL",
        "EUPL",
        "CC-BY-NC",
        "CC-BY-SA"
      ],
      "why": "Everything in the production closure can end up inside something we distribute — the open core is published and the closed modules ship as build artefacts. Weak copyleft (MPL/LGPL) is as unacceptable here as strong copyleft: it becomes a licence question the moment the file is distributed. This closure is empty today because ADR 0002 keeps `dependencies` empty; the scope is what keeps that true when somebody adds the first one (ADR 0060)."
    },
    "development": {
      "allowed": [
        "MIT",
        "Apache-2.0",
        "BSD-2-Clause",
        "BSD-3-Clause",
        "ISC",
        "0BSD",
        "Unlicense",
        "CC0-1.0",
        "MPL-2.0"
      ],
      "forbidden": [
        "GPL",
        "AGPL",
        "EPL",
        "CDDL",
        "SSPL",
        "BUSL",
        "EUPL",
        "CC-BY-NC",
        "CC-BY-SA"
      ],
      "why": "Build- and test-time only; nothing here is redistributed (measured: 0 production packages). File-level copyleft is acceptable at this boundary because it does not travel into our sources — MPL-2.0 arrives with the lightningcss platform binaries under the Vite/Vitest toolchain, and that is a decision with a reason instead of a surprise on the next `npm ci`. Strong copyleft stays out: a GPL build tool raises the question of what its output is derived from, and this repository would rather not have to answer it."
    },
    "exceptions": []
  },
  "topics": {
    "uds": {
      "title": "UDS / protocol layer",
      "summary": "Adding or changing a UDS service, NRC handling, session control or the link seam between protocol and transport.",
      "packages": [
        "@vdp/protocols-uds",
        "@vdp/protocols-kwp2000",
        "@vdp/protocols-oem",
        "@vdp/transport-iso-tp",
        "@vdp/transport-can",
        "@vdp/core"
      ],
      "adrs": ["0013", "0039", "0041"],
      "docs": ["docs/api/uds.md", "docs/api/transport.md"],
      "flows": ["docs/flows/diagnostic-read.md", "docs/flows/diagnostic-write.md"],
      "examples": ["tests/examples/diagnostic-read.example.ts"]
    },
    "transport": {
      "title": "Transport / adapter layer",
      "summary": "Adding a CAN adapter, changing ISO-TP segmentation, or wiring a new physical link into the bus contract.",
      "packages": [
        "@vdp/transport-can",
        "@vdp/transport-iso-tp",
        "@vdp/transport-doip",
        "@vdp/adapter-generic-can",
        "@vdp/adapter-elm327",
        "@vdp/adapter-canable",
        "@vdp/adapter-socketcan",
        "@vdp/adapter-host"
      ],
      "adrs": ["0001", "0013"],
      "docs": ["docs/api/transport.md"],
      "flows": ["docs/flows/diagnostic-read.md"],
      "examples": []
    },
    "diagnostic-ir": {
      "title": "Diagnostic IR",
      "summary": "Observations with provenance: adding a new observation shape, or changing how evidence, DTC or session data are represented between raw form and projections.",
      "packages": [
        "@vdp/diagnostic-ir",
        "@vdp/core",
        "@vdp/runtime",
        "@vdp/reports",
        "@vdp/ai"
      ],
      "adrs": ["0033", "0034", "0037", "0038"],
      "docs": ["docs/api/diagnostic-ir.md", "docs/api/evidence.md"],
      "flows": ["docs/flows/dtc-analysis.md", "docs/flows/ai-analysis.md"],
      "examples": ["tests/examples/evidence-flow.example.ts"]
    },
    "dtc": {
      "title": "DTC system and variant knowledge",
      "summary": "Fault memory, freeze frames, DTC knowledge per variant, and the scan → observation → evidence chain.",
      "packages": [
        "@vdp/core",
        "@vdp/diagnostic-ir",
        "@vdp/definitions",
        "@vdp/runtime"
      ],
      "adrs": ["0023", "0024", "0025", "0026", "0037", "0049"],
      "docs": ["docs/api/diagnostic-ir.md", "docs/api/runtime.md"],
      "flows": ["docs/flows/dtc-analysis.md"],
      "examples": ["tests/examples/dtc-analysis.example.ts"]
    },
    "simulator": {
      "title": "Simulator and replay",
      "summary": "Vehicle behaviour model, scenario engine, fault injection, golden sessions and recording/replay.",
      "packages": [
        "@vdp/simulators",
        "@vdp/golden-sessions",
        "@vdp/protocols-uds",
        "@vdp/core",
        "@vdp/definitions",
        "@vdp/transport-can",
        "@vdp/transport-iso-tp"
      ],
      "adrs": ["0005", "0036", "0039", "0040", "0041"],
      "docs": ["docs/api/uds.md"],
      "flows": ["docs/flows/recording-replay.md"],
      "examples": ["tests/examples/simulator-scenario.example.ts"]
    },
    "ai": {
      "title": "AI analysis layer",
      "summary": "Analysis providers, the evidence contract of the analysis input, citations and versioned answers.",
      "packages": ["@vdp/ai", "@vdp/diagnostic-ir", "@vdp/core", "@vdp/runtime"],
      "adrs": ["0033", "0038"],
      "docs": ["docs/api/evidence.md", "docs/api/hypothesis.md"],
      "flows": ["docs/flows/ai-analysis.md"],
      "examples": ["tests/examples/evidence-flow.example.ts"]
    },
    "harvest": {
      "title": "Harvest: read-only vehicle read-out, ODX and observed data",
      "summary": "Reading a vehicle without changing it: the harvest plan, the DTC read order (0x19 0x01/0x03/0x02/0x04/0x06), the observation record, the ODX-D/PDX projection and its cross-check against odxtools, and the definition candidate whose provenance is \"observed\".",
      "packages": [
        "@vdp/harvest",
        "@vdp/protocols-uds",
        "@vdp/core",
        "@vdp/definitions",
        "@vdp/diagnostic-ir",
        "@vdp/storage"
      ],
      "adrs": ["0002", "0003", "0024", "0033", "0049", "0058"],
      "docs": ["docs/api/uds.md", "docs/glossary.md", "tools/harvest/README.md"],
      "flows": ["docs/flows/harvest.md", "docs/flows/dtc-analysis.md"],
      "examples": ["tests/examples/diagnostic-read.example.ts"]
    },
    "formal": {
      "title": "Formal reference and conformance vectors",
      "summary": "Changing the ISO-TP transport or the write-safety chain, the Haskell reference models in formal/, or the shared test vectors both are graded against.",
      "packages": ["@vdp/transport-iso-tp", "@vdp/core", "@vdp/formal-conformance"],
      "adrs": ["0045", "0050", "0051", "0052", "0053"],
      "docs": ["docs/api/transport.md", "docs/api/runtime.md"],
      "flows": ["docs/flows/diagnostic-write.md"],
      "examples": []
    }
  }
}
