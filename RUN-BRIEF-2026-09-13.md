# Nachlauf-Brief, gefüllt — Stand 2026-09-13, Basis `b93f236`

Alle Zahlen hier sind in diesem Checkout **gemessen**, nicht übernommen. Prüfbar mit
den Kommandos je Punkt; das Messprotokoll dieses Laufs steht in §0.

**Vorwort zur Umgebung.** Beim Öffnen dieses Sandboxes fehlten `node_modules`, `dist/`,
`coverage/` **und** `sessions-local/` (allesamt ignoriert und zwischen Sitzungen weggeräumt —
an `sessions-local/` scheitert sonst jeder „die Datei beweist es"-Nachweis nach dem ersten
Turn). `npm ci` (3,4 s) und `npm run build` (~4 s) sind daher **Schritt 0 jedes Laufs**, und
die Demo muss neu gestartet werden, bevor `session.json` existiert — Regel 34.26: ein Lauf
gegen fehlendes `dist` ist kein Beleg. „Deps installiert, Demo läuft" galt zum
Schreibzeitpunkt des Entwurfs, nicht für diesen Stand.

---

## 0. Messprotokoll dieses Laufs

| Messung | Ergebnis |
|---|---|
| `npm ci && npm run build` | grün |
| `npm run test:coverage` (unit+protocol+regression+replay+integration+architecture) | **EXIT=0**, 90 Dateien, **1300 Tests**, 31,31 s (mit Coverage), global 96,48 St / 89,67 Zw / 97,52 Fn / 97,86 Zl |
| Runner-Nebenbefund | „at least ~8.86 s faster with `isolate: false`" (E14, heute nachgemessen; AGENTS nannte ~7,72 s) |
| Demo am lebenden Objekt | `npm run demo` → `http://127.0.0.1:8080`, Simulator verbunden; `/api/vehicle/resolve` → „Virtual Simulator vehicle (SIM-1) — 100 % belegt"; `/api/session/save` → `sessions-local/session_mtzylh7p_1/session.json` |
| Bericht live | `/api/export/report.html`: Untertitel `2003 (1HGCM82633A004352)`, Recommendations = **zwei** × `inspect before further use` |
| Analyse live | `/api/analyze` (heuristic, confidence 0,4): Titel `C0035 stored in ABS / Brake Control Unit` usf., kein Fahrzeug in der Antwort |
| Web-Schicht unter Coverage | `npx vitest run --coverage --project unit --project integration --coverage.include 'apps/web/src/**/*.ts' --coverage.include 'tools/**/src/**/*.ts'` → `apps/web/src` 79,97 / 72,6 / 80,79 / 82,25; `tools/definition-importer` 95,32 / 70,9 / 100 / 97,89 |

---

## 1. Korrekturen am Entwurf (Belege, die nicht hielten)

1. **`VehicleSessionData` hat sehr wohl ein Fahrzeugfeld.** `packages/core/src/session/session.ts:91`
   deklariert das Interface, `:95` trägt `vehicle?: VehicleIdentity`, `:102` die
   `dtcSnapshots`. Die Zeile „hat kein Feld" ist falsch. Falsch ist auch „wird nicht
   persistiert": `repository.save` serialisiert das gesamte `VehicleSessionData`
   (`packages/storage/src/repository.ts:95-99`, ein `JSON.stringify(data)`), gemessen an
   `session.json` — `vehicle` steht drin. **Was fehlt, ist die Bestimmung, nicht das Fahrzeug.**
2. **Die DTC-Anreicherung liegt schon auf der Platte.** `engine.ts:491`/`:579` schieben
   `EnrichedDtc`-Objekte in ein `DtcRecord[]`, der Typ filtert sie nur im Quelltext, nicht
   im JSON. Gemessen an `sessions-local/session_mtzylh7p_1/session.json`,
   `dtcSnapshots[-1].records[0]`, keys: `code, raw, failureType, status, statusBits, severity,
   description, hint, relatedSignals, ecuName, ecuId, knowledge, firstSeen, lastSeen,
   firstSeenInThisScan`. Von 8 Codes tragen 6 ein `hint`, 5 ein `knowledge`
   (`vehicle`, `vehicle-engine`×2, `vehicle-gearbox`×2), U0121 `scope: "package"` + 1 Note,
   C1234 nichts. Punkt 1 ist damit **Typ + Leser + Bestimmung**, nicht „Daten erfinden".
3. **Der Reportsatz „fällt auf … zurück" ist eine verlorene Zeile am Aufrufort, keine
   Wissenslücke.** `ReportDtc` hat `hint` (`packages/reports/src/report.ts:25`), `DtcView`
   auch (`apps/web/src/backend.ts:193`), und `apps/web/src/server.ts:368-379` mappt beim
   Bericht genau dieses Feld nicht. Live belegt: P0300 druckt `inspect before further use`,
   obwohl derselbe Record ein dokumentiertes `hint` und 3 Muster trägt.
4. **Layering, das Punkt 1/2 zwingend einhalten müssen:** `@vdp/reports` darf nur
   `@vdp/core` importieren, `@vdp/storage` nur shared+core, `@vdp/domain` nur shared
   (`tests/architecture/dependencies.test.ts:108,164,165`). `VehicleCandidate`/`ResolutionEvidence`
   (definitions) und `VehicleCandidateRef` (domain) sind von Report und Storage **nicht
   erreichbar**. Die erlaubte Form ist die flache Kern-Projektion — genau das Muster, das
   `DtcVariantKnowledge` (`packages/core/src/dtc/scanner.ts:54-77`) vorgemacht hat.
5. **Kein Schema-Bump nötig — und ein Bump wäre hier teuer, nicht Formalie.**
   `SESSION_SCHEMA_VERSION = 1` (`packages/core/src/session/session.ts:14`),
   `defaultMigrations = []` (`packages/storage/src/migrations.ts:100`), und `migrate()`
   wirft für jede Version ohne registrierten Schritt (`:69`). Ein Bump auf 2 ohne Schritt
   macht **jede gespeicherte Sitzung unlesbar**. Rein additive optionale Felder brauchen
   keinen Bump; das ist die messbare Begründung hinter der Verbotsregel.
6. **Punkt 4 ist eine Coverage-Lücke, keine Typecheck-Lücke.** `apps/web/src/**/*.ts`
   bauen über `apps/web/tsconfig.json` in der Solution (`tsc -b` grün), `apps/web/public/*.js`
   laufen durch `tsconfig.frontend.json` mit `checkJs: true`, die Web-Tests durch
   `tsconfig.typecheck.json`. Wer hier „Typecheck nachrüsten" will, repariert eine
   nicht existierende Lücke. Zusätzlich gemessen: `tools/**/src/**/*.ts` verfehlt
   `tools/test-reporters/flaky-reporter.ts` (liegt nicht unter `src/`) und zieht
   `tools/simulators/src/index.ts` mit 0 % ein, weil die Barrel-Ausnahme nur
   `packages/**/src/index.ts` kennt (`vitest.config.ts:187-190`).
7. **E10 ist in diesem Checkout anders, als AGENTS schreibt.** `git ls-files .github/workflows`
   → **nur `ci.yml`**. Die vier gehärteten Dateien liegen hier nicht in der Arbeitskopie;
   0.E E10 behauptet das Gegenteil (Verstoß gegen 34.24, im selben PR zu korrigieren).
   `retry: 2` steht unverändert (`vitest.config.ts:121`), eine `isolate`-Zeile existiert
   nicht (Default Isolation an, E14 offen).
8. **Der Slot für die Bestimmung existiert bereits und ist permanent leer:**
   `VehicleSummary.vehicleId` (`packages/domain/src/model.ts:23`) wird von
   `toVehicleSummary` (`packages/runtime/src/mappers.ts:61-74`) nie gefüllt — gemessen:
   das `return`-Objekt kennt kein `vehicleId`. `services.ts:234` nimmt `this.resolve().best`
   ausschließlich, um den Scanner zu binden. Punkt 1 hat damit eine natürliche Senke.
9. **Zwei Funde, die Punkt 2 und 6 schärfen (beide live reproduziert).**
   (a) `pdf.ts` faltet Nicht-Latin-1 über `mapUnicode` (`:210-236`); `…` (0x2026) und `—`
   (0x2014) laufen ins `default: "?"`. Gemessen: `"45…55 km/h · ≥ 60 °C — Kühlung"` wird zu
   `"45?55 km/h · >= 60 °C ? Kühlung"`. Das Wissens-Vokabular des Simulator-Pakets benutzt
   `…`/`—` 31 mal, `report.ts` setzt `"—"` 9 mal als Leerwert. Ein Bericht, der Wissen zitiert,
   **muss** die Faltung nachziehen, sonst exportiert er Fragezeichen (dieselbe Klasse wie ADR 0021).
   (b) `tools/definition-importer` `importJson` (`:223-235`) liest nur `ecus` und `signals`.
   Auf einem Fixture mit `vehicles[].dtcKnowledge[]`: `pkg.vehicles === undefined`,
   `valid: true`, `errors: []` — das Variantenwissen verschwindet lautlos und der Import
   meldet Erfolg. Genau die Klasse des `notes`-Verlusts aus ADR 0025.

---

## 2. Punkt 1 — Fahrzeugbestimmung wird Sitzungsdatum

**Anker:** `packages/core/src/diagnostics/engine.ts:405` (`createIdentityFromVin`), `:408`
(Zuweisung), `packages/core/src/vehicle/identity.ts:26-41` (füllt nur `vin`, `vinAnalysis`,
`modelYear`, `ecus`, `attributes`), `packages/core/src/session/session.ts:91-108`,
`packages/runtime/src/services.ts:223-239` (Auflösung → nur Scanner-Bindung),
`packages/runtime/src/mappers.ts:61-74`, `apps/web/src/backend.ts:423` (`private resolution?`
— der einzige Ort, an dem die Bestimmung heute lebt), `:818-821`.

**Zu tun (drei Schritte, dieselbe Datenkette, kein viertes Ablagegesetz):**

- **1a Typ.** `dtcSnapshots[].records` von `DtcRecord[]` auf `EnrichedDtc[]`
  (`session.ts:102`, `addDtcSnapshot` `:257-269`; `EnrichedDtc` ist über
  `packages/core/src/index.ts:14` bereits öffentlich). Kein neues Record-Schema, keine
  neuen Felder — der Typ beschreibt, was der Scanner erzeugt. `firstSeenInThisScan`
  ist eine Scan-lokale Markierung und wird **nicht** persistiert (sonst steht nach dem
  Laden „neu in dieser Sitzung", wo „neu in diesem Scan" gemeint war); der Felderverlust
  ist als Regressionseintrag zu katalogisieren (Symptom + Biss).
- **1b Bestimmung.** Eine flache Kern-Form `VehicleDetermination` (Ort: `packages/core/src/session/`,
  benannt wie `DtcVariantKnowledge`, mit Verweis auf §11.1 im Dateikopf) mit genau dem, was
  ein Leser braucht: `oem`, `packageKey`, `packageVersion`, `vehicleId`, `brand`, `model`,
  `platform`, `modelYear`, `score` (0…1), `weights {support,conflict,evaluated}`, `trust`,
  `provenanceType`/`provenanceSource`, `evidence`/`conflicts` (Kurzformen nach §11.1),
  `unresolved`, `notes`. Geschrieben an **einer** Stelle: `services.ts:234`, wo der
  beste Kandidat schon in der Hand liegt (Connect und `resolve(hints)` laufen über denselben
  Pfad; `disconnect` löscht, wie §20.1 die Scannerbindung auch löst). Additiv, optional →
  **kein** Schema-Bump; ADR 0026 (neue Nummer, nur für die Vertragsergänzung).
- **1c Identität vollenden.** Die vom Kandidaten gelieferten Marken-/Modellfelder in
  `session.data.vehicle` schreiben (`identity.ts:26` nimmt `extra` längst an), damit
  `describeVehicle` und der Bericht das Auto nennen statt `2003 (1HG…)`.

**DoD:** 1a–1c grün in `npm run ci`; eine Storage-Round-Trip-Test deckt „Speichern → Laden →
Bestimmung noch da" ab (neben `session.spec.ts`); `toVehicleSummary` füllt `vehicleId`;
UI unverändert (sie zeigt die Auflösung bereits, nur die Datei wusste nichts);
AGENTS 0.A, §10/§11 und README nachgezogen (34.24); Demo-Beweis im PR-Body.

**Nachweis:**
```bash
grep -c 'records: DtcRecord\[\]' packages/core/src/session/session.ts   # heute 3 (Zeilen 102, 258, 260) → Ziel 0
grep -c 'vehicleId' packages/core/src/session/session.ts                  # heute 0 → Ziel ≥1
grep -n 'vehicleId' packages/runtime/src/mappers.ts | sed -n '1,3p'        # heute nur :126 (knowledge)
# lebend: demo → /api/dtc/scan → /api/session/save, dann
node -e 'const d=JSON.parse(require("fs").readFileSync(require("child_process").execSync("ls -t sessions-local/*/session.json|head -1").toString().trim()));console.log(Object.keys(d.vehicle),!!d.determination,d.dtcSnapshots.at(-1).records[0].firstSeenInThisScan)'
# heute: [ecus,vin,vinAnalysis,modelYear,attributes] false true → Ziel: +brand/model, true, undefined
```

**Kein Erfolg:** eine zweite Wissensdatei neben `session.json`; `vehicleId` nur im View,
nicht in der Datei; Persistieren, was die Workbench ohnehin neu würfeln kann (Rohliste aller
Kandidaten statt der getroffenen Bestimmung); `firstSeenInThisScan` als Sitzungs­wahrheit.

---

## 3. Punkt 2 — Der Bericht kennt das Variantenwissen

**Anker:** `packages/reports/src/report.ts:20-26` (`ReportDtc`, `hint` auf Zeile 25), `:62` (Untertitel),
`:82-112` (`vehicleSection`), `:159-175` (`dtcSummarySection`, Spalten Code/Severity/ECU/
Description), `:247-263` (`defaultRecommendations`, Fallback `:253`),
`apps/web/src/server.ts:368-379` (Aufrufort, `hint` fällt weg), `packages/reports/src/pdf.ts:210-236`
(Faltung).

**Zu tun:** 2a `hint` am Aufrufort durchreichen (eine Zeile, Typ existiert). 2b DTC-Tabelle
um `scope` und das dokumentierte Erste-Prüfen-Fenster erweitern, **gespeist aus dem Record**
(aus 1a/1b), nicht aus `@vdp/definitions`. 2c `defaultRecommendations`: bei vorhandenem
Muster Ursache + Reihenfolge + Messfenster zitieren, sonst bleibt der Fallback — und der
Fallback bleibt als Fallback lesbar (kein Umschreiben zu „kein Wissen gefunden" ≠ „nichts zu tun").
2d `vehicleSection` bekommt die Bestimmung (score, Belegzahl, trust/provenance,
`unresolved`-Grund). 2e `mapUnicode` zieht `…` → `...` und `—` → `-` nach, mit Byte-Test
(analog ADR 0021), sonst exportiert der Bericht `45?55 km/h`.

**DoD:** Der live generierte Bericht nennt für P0300/P0420/P0700/P0715 Scope und Fenster,
für U0121 ausdrücklich „nur paketweit" samt Note, und für C1234 bleibt alles beim alten;
Bericht ohne Fahrzeug und ohne Wissen ist byte-identisch zum jetzigen (Test auf Fixture);
`reports`-Gate hält (100/79,5), die 39 ungenommenen Zweigarme an 55-58/91-111/128-152/173/194/205
werden nicht durch Ausnahmen, sondern durch Tests ruhiger; PDF-Test deckt Umlaute **und**
die neuen Zeichen ab; Doku (AGENTS §21-Zeile, 0.A) nachgezogen.

**Nachweis:**
```bash
curl -s localhost:8080/api/export/report.html | grep -c 'inspect before further use'
# heute 2 (U0121 und P0300) → Ziel 1, und zwar die Zeile mit U0121
curl -s localhost:8080/api/export/report.html | grep -c 'vehicle-engine'   # heute 0 → Ziel ≥2
node --input-type=module -e 'const {buildReport,renderPdf}=await import("./packages/reports/dist/src/index.js");/* Fixture mit „45…55 km/h — Kühlung" */'  # heute „45?55 … ? Kühlung" → Ziel „45...55 … - Kühlung"
```

**Kein Erfolg:** Wissen im Bericht ohne Provenance/Notes (dann ist es eine Behauptung);
ein Schönschreib-Renderer, der fehlendes Wissen mit paketweiter Beschreibung füllt;
PDF, die `?` zeigt; eine Reportschicht, die `@vdp/definitions` importiert (Architekturtest).

---

## 4. Punkt 3 — Die Analyse weiß, welches Fahrzeug sie beurteilt

**Anker:** `apps/web/src/backend.ts:994-1021` (`analyze()`, ohne `vehicle`, ohne Wissen),
`packages/ai/src/types.ts:21-27` (`AnalysisDtc`) und `:31` (`vehicle?` — Feld da, nie gefüllt),
`packages/ai/src/heuristic.ts` (153 Zeilen; `:41` `${code} stored in ${ecu}`, `:104-106`
Fallback, `:139-141` `severityOf`-Default), `packages/reports`-Seitenblick: `sessionData()`
liegt in `backend.ts:1147` und wäre die saubere Quelle statt `this.dtcs` (Präsentation).

**Zu tun:** `AnalysisInput` aus derselben Kette speisen wie der Bericht: `vehicle` aus 1b
(`vin` nur, wenn der Aufrufer opt-in sagt — `types.ts:30` und AGENTS 27), plus je DTC
optional `scope`, `conditions`, Anzahl Muster und die auswertbaren Fenster. Der Heuristik-Provider
zitiert das: Titel/detail nennen Fahrzeug und Scope, eine Empfehlung mit dokumentiertem Erstschritt
darf nur dann als Empfehlung erscheinen, wenn `measurable` — sonst bleibt sie Hypothese (§22:
Fact/Observation/Hypothesis/Recommendation). Keine neuen Provider, kein LLM, keine
Netzwerk-Konfiguration, keine Engine-Methode.

**DoD:** `npm run ci` grün; `ai.spec.ts` (heute 21 Tests) deckt die neuen Zweige ab und
`heuristic.ts` verlässt E16 (94,87 / 79,48, 19 ungenommene Arme) mit **Tests**, nicht mit
Gate-Absenkung; der leere `vehicle`-Fall bleibt erste Klasse (Analyse ohne Bestimmung muss
sichtbar sagen, dass sie nichts über das Auto weiß); Doku: §22-Zeile in 0.A.

**Nachweis:**
```bash
sed -n '994,1022p' apps/web/src/backend.ts | grep -c vehicle   # heute 0 → Ziel ≥1
curl -s -XPOST localhost:8080/api/analyze | grep -c 'Virtual Simulator'   # heute 0 → Ziel ≥1
curl -s -XPOST localhost:8080/api/analyze | grep -c 'stored in'           # heute 8 → Ziel: Titel bleibt, detail nennt scope
```

**Kein Erfolg:** Konfidenz, die mit Wissen steigt, ohne dass Belege zitiert werden; eine
Analyse, die `Fehlertyp 0x00`-Füllwörter aus der View fortschreibt (beobachtet:
`toDtcView` erfindet diesen Text bewusst — der Punkt ist die Quelle der Analyse, nicht dieser String);
Prompt-Bastelei in `apps/web`.

---

## 5. Punkt 4 — Web-Schicht und Tools werden gemessen

**Anker:** `vitest.config.ts:186` (`include: ['packages/**/src/**/*.ts']`), `:187-195`
(Ausnahmen), `:196+` (Schwellen, per Glob), `:121` (`retry: isCi ? 2 : 0`),
Unit-Projekt `:160` (`tools/**/src/**/*.spec.ts`), Integration `:167`
(`apps/web/test/**/*.spec.ts`, 77 Tests: 18+11+21+12+15).

**Zu tun:** `apps/web/src/**/*.ts` und `tools/**/*.ts` (unter Ausschluss der Specs) in die
Coverage-`include` aufnehmen; die Barrel-Ausnahme von `packages/**/src/index.ts` auf
`**/src/index.ts` erweitern **mit Begründung im Kommentar** (sonst ist es die in §11
verbotene Ausnahmemechanik); `tools/test-reporters/flaky-reporter.ts` einbeziehen (heute
unerreichbar, weil nicht unter `src/`). Dann pro Glob eine **Bodenschwelle** aus der Messung
(Hebel: `apps/web/src/**` lines 69 / branches 65 — unter dem gemessenen Minimum von
`server.ts` 69,63/65,23, damit das Tor beißen kann, ohne Rot zu erfinden), plus je Datei,
die unter ihrem Glob liegt, eine Zahl im Kommentar. `tools/**` ohne Gate lassen, wenn die
0 %-Datei `simulators/src/index.ts` sonst zur Ausnahmedebatte führt — Entscheidung in den PR.

**DoD:** `npm run test:coverage` grün mit erweiterter include; im PR-Body die Tabelle
vorher/nachher je Datei (Ist-Werte stehen in §0); **keine** Datei neu ausgenommen
(`git diff -- vitest.config.ts` zeigt im Diff keinen `exclude`-Zuwachs außer der begründeten
Barrel-Regel); Suite zählt nicht runter (1300 → ≥ 1300).

**Nachweis:**
```bash
sed -n '184,196p' vitest.config.ts | grep -c "apps/web"   # heute 0 (Coverage-Block) → Ziel ≥1
grep -c "apps/web" vitest.config.ts   # heute 1 — nur die Test-Includes des Integration-Projekts, nicht die Coverage
git diff -- vitest.config.ts | grep -c '^+.*exclude'      # Ziel 0 (außer der dokumentierten Barrel-Zeile)
```

**Kein Erfolg:** Coverage steigt, weil Dateien ausgenommen oder verschoben werden;
`apps/web/src` bekommt eine eigene Suite auf Kosten der Integrationstests; die globalen
Schwellen (90/80/90/90) werden angetastet.

---

## 6. Punkt 5 — Schulden aus 0.E, am selben Baum nachgemessen (2026-09-13)

| Eintrag | Gemessen heute | Was der Lauf damit macht |
|---|---|---|
| **E15** (P3) | `apps/web/src/backend.ts` **1336** Z. (größtes Modul), `runtime/services.ts` 726, `iso-tp/connection.ts` 724, `public/app.js` 1009. Rest-Präsentation in `backend.ts`: `DtcView`/`EcuView`-Typen `:177-215`, `toDtcView` `:1161-1183`, `toSampleView`/`toMarkerView`/`toTraceView` `:1246-1290`. Bereits ausgelagert und als Schnitt belegt: `vehicle-view.ts` 235, `dtc-knowledge-view.ts` 200, `runtime/vehicle-resolution.ts` 112. | Im Zug von Punkt 1–3 mitnehmen: die View-Mapper wandern als **Umzug ohne Verhaltensänderung** in `apps/web/src/dtc-view.ts`/`ecu-view.ts`; Punkt 1c/2/3 berühren `toDtcView` ohnehin. Architektur-Allowlist unverändert lassen. |
| **E16** (P2) | Unverändert reproduzierbar: `elm327/protocol.ts` 96,29/76,0 (+11,3/+1,0, Arm 13, Zeile 105), `transport/can/bus.ts` 87,5/100 (+2,5, Zeile 39, Arm 44), `elm327/stream.ts` 88,23/100 (+3,2, Zeilen 47,60), `reports/report.ts` 100/79,48 (+5,0/+4,5, 39 Arme), `ai/heuristic.ts` 94,87/79,48 (+4,9/+4,5, 19 Arme, Zeilen 105/141). | Nur die zwei Dateien anfassen, die Punkt 2/3 ohnehin öffnen (`report.ts`, `heuristic.ts`): die ungenommenen Arme sind Optional-/Fallback-Zweige (55-58, 91-111, 128-152 / 104, 141) — Tests, dann Gate. Für `protocol.ts`, `stream.ts`, `bus.ts` gilt: **kein** Gate-Rubbeln, keine Rahmen-Tests, die nichts behaupten; wenn nicht belegt, bleibt der Eintrag offen. |
| **E11** (P2) | `ecu-session.ts` 85,58/71,26 → **+0,58** über `core`-Gate 85/65; ungenommen Zeilen 173-179, 194, 234, 239, 251, 263, 289, 314, 394, 404, 426 (44 Arme). | Nur nachtesten, wenn Punkt 1 die Datei berührt (1c könnte). Sonst: Eintrag stehen lassen, Zahl im Befund auf 2026-09-13 nachziehen (34.24). |
| **E9** (P3) | `vitest.config.ts:121` `retry: isCi ? 2 : 0` — unverändert. | Nicht antasten: der Beleg fehlt, weil E10 fehlt (34.21). |
| **E14** (P3) | Keine `isolate`-Zeile in der Config (Isolation an). Runner meldet heute ~8,86 s Gewinn. | Entscheidung oder Verwurf, dokumentiert — nicht stillschweigend übernehmen; gehört nicht in diesen PR. |
| **E10** (P1) | `git ls-files .github/workflows` → nur `ci.yml`. AGENTS 0.E behauptet, die vier gehärteten Dateien lägen in der Arbeitskopie. | Doku-Korrektur im PR (34.24): E10-Befund auf „nicht vorhanden, muss neu entwickelt werden" oder Dateien nachliefern — beides ist eine Nutzerentscheidung (GitHub-App → Workflows: Read & write). `npm run ci` bleibt das verbindliche Tor. |

**DoD Punkt 5:** Jede Zahl im Backlog, die dieser Lauf zitiert, trägt ihr Messdatum; jede
geschlossene Schuld entfernt ihren Eintrag **mit** dem Nachziehen von 0.A/README/CONTRIBUTING;
keine Zahl wird „gefühlt" aktualisiert.

---

## 7. Punkt 6 — §23: die zwei ⏳ als Prüfung auf dem Importweg

**Anker:** AGENTS §23 (Zeile `Legal/Licensed Documentation ⏳`, Zeile `Community Knowledge ⏳`),
`packages/definitions/src/validate.ts:127-183` (`validateProvenance`), aufgerufen auf drei
Stufen: Paket `:82`, Fahrzeug `:199`, Wissenseintrag `:365`; `json.ts:231-394, 505-529`
(vollständiger Parser inkl. `vehicles[]`/`dtcKnowledge[]`/`patterns[]`/`checks[]`),
`tools/definition-importer/src/index.ts:223-235` (`importJson`, liest nur `ecus`/`signals`),
`tests/regression/regression.test.ts:387` (Nachbareintrag: `notes` gingen auf dem Dateipfad
verloren).

**Zu tun (Prüfung, kein Feature):** Den doppelten, schwächeren Parser schließen — `importJson`
delegiert die Kodierung an `parseDefinitionPackageJson` aus `@vdp/definitions` (Regel 34.2) und
legt nur seine Optionen darüber; **oder**, wenn der Werkzeugweg bewusst nur ECU/Signal-Listen
kennt, dann darf ein Quelltext, der `vehicles` erklärt, nicht mit `valid: true` durchlaufen:
ein deklariertes, aber ignoriertes Schlüsselwort ist ein Fehler, kein Feature. Auf demselben
Weg werden die Gates geprüft, die es schon gibt: `licensed` ohne `license` schlägt auf dem
Dateipfad fehl (nicht nur am Objekt), `community` mit `repair` warnt auf dem Dateipfad,
`retrievedAt`-Quatsch ist auf beiden Wegen ein Fehler. §23-Tabellenzeilen:
`Legal/Licensed Documentation` kann auf ✅, sobald die **Präsenz**prüfung auf dem Dateipfad
greift (die inhaltliche Bewertung, welche Lizenz weitergegeben werden darf, bleibt ⏳ mit
benanntem Grund — eine Lizenz-Allowlist wäre eine Entscheidung, keine Messung); `Community
Knowledge` bleibt ⏳, aber **geprüft**: ein Test hält fest, dass es keinen Eingabepfad gibt und
nichts vorgaukelt (kein `community`-Text ohne Warnung in Report/UI).

**DoD:** Regressionseintrag „a file-imported package lost its `vehicles[]` section" mit Symptom
und Biss (Probe: Übertragungszeile auskommentieren → `1 failed`); +3 Importer-Specs (Wissen
überlebt, Lizenzfehler auf Dateipfad, Community-Warnung auf Dateipfad); §23-Zeilen mit Datum;
`definition-importer` bleibt unter seinen Tests sichtbar (Punkt 4 misst die Datei jetzt).

**Nachweis (heutiger Ausgabewert ist der Befund):**
```bash
node --input-type=module -e '
const t=await import("./tools/definition-importer/dist/src/index.js");
const src=JSON.stringify({schemaVersion:3,ecus:[{id:"engine",name:"E",protocol:"uds",address:{txId:0x7e0,rxId:0x7e8},dtcs:[{code:"P0420",description:"cat"}]}],signals:[],vehicles:[{id:"v1",brand:"B",model:"M",engines:[{id:"e1",name:"n"}],dtcKnowledge:[{code:"P0420",engine:"e1",description:"variante"}]}]});
const r=t.importJson(src,{oem:"acme",name:"x",provenance:{sourceType:"licensed",source:"s",license:"MIT"}});
console.log("vehicles:",JSON.stringify(r.pkg.vehicles??null),"| valid:",r.valid,"| errors:",r.errors.length);'
# heute: vehicles: null | valid: true | errors: 0  → Ziel: vehicles: 1, knowledge: 1
```

**Kein Erfolg:** ein dritter Parser; eine Lizenz-Allowlist aus dem Bauch; `community`-Wissen
als neue Datenquelle andocken; §23 auf ✅ schreiben, ohne dass der Dateipfad prüft.

---

## 8. Reihenfolge und warum

`1 → 2 → 3` (dieselbe Kette: Record-Typ → Bericht → Analyse; jeder Schritt ist ohne den
vorherigen nur Dekoration), dann `4` (messen, was 1–3 bewegt haben), dann `5` (die Schulden,
die in denselben Dateien wohnen), zuletzt `6` (Prüfung auf dem Importweg, unabhängig von 1–5,
aber mit derselben Begründungsform). Ein PR je Punkt; `npm run ci` vor jedem Commit.

## 9. Hausregeln (verbindlich, mit Regelnummer)

- **34.21** Messung vor Behauptung — jede Zahl in diesem Brief ist nachprüfbar; eine neue
  Behauptung braucht einen Lauf.
- **31.6** Regressionseintrag mit Symptom und belegtem Biss (20 Einträge im Katalog,
  `tests/regression/regression.test.ts`); „Biss" heißt: kurz auskommentieren → `1 failed`.
- **34.24** Doku-Wahrheit — Abweichung AGENTS/ADR ↔ Baum wird im selben PR korrigiert
  (hier: E10, §23, 0.A, README-Testzahl).
- **UI ohne Vokabular** — Rendern ja, Benennen nein; Labels bleiben in `*-view.ts` gegen
  die Union-Typen typisiert (`app.js` 1009 Zeilen, davon null deutsche Wörter).
- **Demo am lebenden Objekt** — `npm run demo`, dann `curl` gegen 127.0.0.1:8080;
  jeder Punkt hat oben sein Live-Kommando mit heute gemessenem Ausgabewert.
- **34.26/34.21** erst `npm ci && npm run build`, dann messen.
- **35** Definition of Done je Punkt, PR-Template ausgefüllt (`.github/pull_request_template.md`).

## 10. Explizit verboten (damit „nicht erweitern" trägt)

Schritt 16 / geführte Diagnose · Coding, Adaption, Servicefunktionen · neue Adapter oder
Protokolle · LLM-Anbindung · UI-Redesign · erfundene Fahrzeugwahrheit · Schema-Bump ohne
Migration + ADR (vgl. §1.5: ein Bump ohne registrierten Schritt macht alle Sessions kaputt) ·
neue öffentliche Engine-Methoden (heute 19) · neue Laufzeit-Abhängigkeiten (ADR 0010) ·
neue Ablageorte für Wissen (AGENTS §23: Wissen liegt im Paket bei den Fahrzeugen).

## 11. Explizit kein Erfolg

- Bericht mit Wissen, aber ohne Provenance/Notes, oder mit `?` im PDF.
- `determination` im View, aber nicht in `session.json` (und umgekehrt: Datei ohne Leser).
- Coverage steigt, weil Dateien ausgenommen, umbenannt oder verschoben werden.
- `backend.ts` schrumpft, weil Logik ins Browser-`app.js` wandert.
- Gate-Anhebungen ohne neue Tests; Testanzahl, die fällt, während „die Suite schneller" wird.
- §23 auf ✅ ohne Prüfkraft auf dem Dateipfad.
- Eine zweite Fahrzeugachse (Resolver/Side-Table/Zweitformat) statt der bestehenden Leitung.

## 12. Beobachtet, nicht beauftragt (bewusst offen, damit der Rahmen hält)

`importJson` wirft einen `TypeError`, wenn `options.provenance` fehlt (nur zur Laufzeit —
typseitig Pflicht, `index.ts:20-27`). · `StoredSessionSummary` zeigt `vin`, aber nicht,
was gematcht wurde (`repository.ts:23-32`) — folgt automatisch, wenn 1b existiert und ein
Load-Test es verlangt; sonst bleibt es offen. · `toDtcView` erfindet `Fehlertyp 0x00` als
Beschreibung (AGENTS 24-konform, aber Presentation als Analysequelle). · `storage`/`reports`
importieren `@vdp/core` (allowlist `:164-165`); das Ablösen setzt die Session-Schemata unter
`domain` voraus — Roadmap-Schritt 10–13, nicht dieser Lauf. · DoIP hat keinen
Server-Mock (`packages/transport/doip/src/` = discovery/message/transport);
E10, E14 bleiben, wie sie sind.
