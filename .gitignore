node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
*.log
# Committed trace fixtures are witnesses, not build output — and `candump -l`
# writes `*.log`, so the fixtures keep the extension a real log has.
!tests/fixtures/traces/*.log
sessions-local/
harvest-local/
coverage/
.ai/generated/
