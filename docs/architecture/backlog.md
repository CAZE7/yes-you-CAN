# 0.E Offene Verbesserungen — Backlog

> **Ausgelagert aus [`AGENTS.md`](../../AGENTS.md) am 2026-09-24** (Eintrag 1.47).
> Dieser Backlog ist bewusst **auf Bestehendes beschränkt: keine neuen
> Funktionen**, jede Maßnahme verbessert Vorhandenes. Ein abgearbeiteter Eintrag
> wird durch den PR entfernt, der ihn behebt — zusammen mit dem Nachziehen von
> 0.A/README/CONTRIBUTING (Regel 34.24). Die Nummerierung 0.E bleibt, damit
> „AGENTS 0.E E10" im Code, in den ADRs und in den Tests weiter auflösbar ist.

## 0.E Offene Verbesserungen — Backlog (Stand 2026-09-14)

Dieser Backlog ist bewusst **auf Bestehendes beschränkt: keine neuen
Funktionen**, jede Maßnahme verbessert Vorhandenes. Prioritäten: **P1** =
Qualität/CI-kritisch, **P2** = Korrektheit/Konsistenz, **P3** =
Hygiene/Refactoring. Jeder Eintrag nennt den Befund mit Messung (Regel
34.21) und die konkrete Verbesserung. Ein abgearbeiteter Eintrag wird durch
den PR entfernt, der ihn behebt — zusammen mit dem Nachziehen von
0.A/README/CONTRIBUTING (Regel 34.24).

**Am 2026-09-11 abgearbeitet (v1.5):** E1 (CI-Härtung real), E2 (`npm test`
baut selbst), E3 (Coverage-Wahrheit + ADR 0017), E5 (6 Ebenen, echter
Frontend-Typecheck), E6 (leeres `catch {}` weg), E7 (Script-Duplikate weg)
und der Storage-Teil von E4 (Gates 70/45 → 90/55 nach Tests).

**Am 2026-09-12 abgearbeitet (v1.6):** E8 (Engine-Zerlegung, Roadmap-Schritte
8/9) — `apps/web` ist engine-frei, vollständiges Command-/Query-Vokabular,
doppelter Live-Start und Doppel-Recording messbar behoben (0 Duplikate),
`dispose()` schließt den Bus auch nach fehlgeschlagenem Connect; Details im
Ergebnisabschnitt der Migrations-Roadmap.

**Am 2026-09-12 abgearbeitet (v1.8):** **E4** (DoIP-Coverage):
`transport/doip/src/transport.ts` 78,6/68,3 → **98,5/88,7**,
`discovery.ts` 91,3/52,9 → **100/78,9**, `charts/group.ts` 77,0/77,6 →
**99,1/91,3**; Gates angehoben (global 90/80, transport 85/70, charts 90/75 —
ADR 0020). Dabei gefunden und behoben: ein fehlgeschlagener Routing-Aktivierung
ließ den DoIP-Transport in `connecting` mit offenem Socket zurück, und
`ChartGroup.notify()` schluckte Subscriber-Fehler. **Nachzug zu E6:** die drei
verbliebenen leeren `catch {}` (`charts/group.ts`, `adapters/canable/adapter.ts`,
`adapters/host/serial.ts`) sind beseitigt — Regel 34.25 ist damit im gesamten
Baum erfüllt (nachgemessen am 2026-09-12: Skript über 211 `*.ts`/`*.js`-Dateien
ohne `node_modules`/`dist`, Kommentare entfernt → **0 leere `catch`-Blöcke**).
**Neu (ADR 0019):** das Discovery-Zeitbudget ist explizit (`probeDelayMs`) und
injizierbar; feste Sleeps in Tests sind durch Bedingungs-Waits ersetzt.
**Messung:** Gesamtlauf 71,69 s → 23,83 s, Suite 971 → 991 Tests grün.

**Am 2026-09-12 abgearbeitet (v1.9):** **E13** vollständig — `storage/zip.ts`
58,1 → **83,9 %** Zweige, `migrations.ts` 61,5 → **92,3**, 22 statt 15 Tests,
Gate 90/55 → **95/80**; die Anhebung war in E13 beabsichtigt, aber nie
committet, und ist jetzt mit ADR 0022 nachgeholt. **E12** ist gegenstandslos:
`adapters/host/catalog.ts` misst seit der SocketCAN-Naht **100 Zeilen / 95,2 %
Zweige** (der Befund nannte 68,0/48,6), das `adapters`-Gate steht auf 85/75.
Dazu zwei Arbeiten ohne Backlog-Nummer, beide mit ADR: der **Latin-1-Fix im
Export-Pfad** (ADR 0021, `pdf.ts` 93,9/71,4 → 100/88,9) und das **Nachtesten
von Analyse und Bericht** samt `messageOf`/`asError` als einem Vertrag in
`@vdp/shared` (ADR 0022, `ai/http.ts` 80,6/78,9 bei 63,6 % Funktionen →
100/87,7 bei 100 %, `ai/service.ts` → 100/100, `errors.spec.ts` neu mit 10
Tests, 40 von 45 Kopien der catch-Zeile ersetzt). **Messung:** Suite 991 →
**1066 Tests** in ~23 s grün (78 Dateien), Zweige global 86,0 → **87,8**, neue per-file-Gates
`shared` 100/95, `reports` 95/75 und `ai` 90/75; dass Gates beißen, ist mit einem absichtlich
unmöglichen Wert geprüft (`EXIT=1`, `pdf.ts` 88,88 % im Fehlertext).

**Am 2026-09-14 abgearbeitet (v1.19):** der Guardrail-Schritt (ADR 0029) —
fünf Regeln `warn`/`off` → `error` ohne Produktionsänderung, drei weitere Regeln
`error` mit fünf behobenen Fundstellen, jede verbleibende `off`-Entscheidung mit
Umfang und Messung im Rekord, und die Quality-Gates laufen jetzt im
`architecture`-Testlauf, weil `ci.yml` sie sonst nie ausführt (E17 bleibt als
Restkopplung dokumentiert). Der 57-Punkte-Backlog des Auftraggebers liegt als
`docs/architecture/master-backlog.md` gegen den gemessenen Stand abgeglichen vor;
die zwei dort korrigierten Vorschläge (kein ESLint, kein Turborepo/Nx auf Vorrat)
sind als Entscheidung in ADR 0029 §1/§5 festgeschrieben und vom Test erzwungen.

Offen bleiben:

| Nr. | P | Befund (gemessen am 2026-09-12) | Verbesserung (Bestehendes verbessern, kein Feature) |
|---|---|---|---|
| E10 | P1 | **Erledigt 2026-09-24, aber nicht von mir — vom Inhaber.** Die vier gehärteten CI-Workflow-Dateien (`ci.yml` mit Quality-Job + Coverage-Upload, `codeql.yml`, `dependency-review.yml`, `hardware.yml`) **liegen im Repository**: sie stehen seit heute auf `main` (`f6abf86`), gepusht vom Repository-Inhaber, nicht von der App. Vorher gemessen, dreifach und immer wortgleich: `remote rejected … refusing to allow a GitHub App to create or update workflow .github/workflows/ci.yml without 'workflows' permission`, und über die Contents-API `403`. **Die Berechtigung der GitHub-App fehlt weiterhin** — ein leerer Probe-Push ohne Workflow-Änderung gelingt, einer mit Änderung an `.github/workflows/` wird abgelehnt. Was damit offen bleibt, ist genau ein Schritt: `ci.yml` lädt `coverage/` mit `if: always()` hoch, führt aber `npm run test:coverage` **nicht als Tor aus** — ein Artefakt, das kommt, ob die Schwellen hielten oder nicht, ist ein Bericht, kein Tor. Der Flaky-Reporter wird von der Matrix weiterhin nicht ausgeführt. | Freischaltung bleibt, was sie war: GitHub → Settings → Applications → Arena (GitHub App) → Repository Permissions → **Workflows: Read & write**. Danach **ein** Schritt in `ci.yml` — `- name: Coverage gates` / `  run: npm run test:coverage` — und die vier Dateien lassen sich über die App pflegen statt nur vom Inhaber. Bis dahin bleibt `npm run ci` das verbindliche Tor (AGENTS 35) und `tests/architecture/coverage-gate.test.ts` der Träger der Coverage-Schwellen innerhalb von `npm test`. |
| E9 | P3 | `vitest.config.ts` erlaubt in der CI weiterhin `retry: 2`. Der Messbeleg für die ursprüngliche Sorge ist seit v1.8 stärker: die Suite brauchte 71,69 s und vier Workbench-Tests warteten mit festen `setTimeout`-Werten (300–400 ms) auf Samples, die der 60-ms-Poll-Loop nach ~70 ms liefert — Rennen unter Last. Feste Sleeps sind jetzt durch Bedingungs-Waits ersetzt, Discovery fährt ein explizites Zeitbudget (ADR 0019), der Lauf dauert 23,83 s. Ein Flaky-Report aus der CI liegt weiterhin nicht vor — **aber der Grund hat sich geändert, und mit ihm die Zahl**: seit 2026-09-24 liegen die Workflows auf `main`, keiner davon ruft den Reporter auf (gemessen: `grep -rn flaky .github/workflows/` trifft nichts). Das Recht (E10) war der Grund, nicht der Zweck; der Zweck fehlt weiter. **Gegen ADR 0027 korrigiert — 0 % standen zuletzt falsch in den Akten:** mit dem Rebase auf `main` kam `tools/test-reporters/src/flaky-reporter.spec.ts` herein (5 Tests, grün), die Datei misst 100 % Statements / 86,66 % Zweige. Getestet ist sie, aufgerufen wird sie nicht. | `retry` schrittweise Richtung 0 senken, sobald ein CI-Lauf der gehärteten Workflows den Flaky-Reporter ausgeworfen hat (`tools/test-reporters/flaky-reporter.ts`). Ohne diesen Nachweis bleibt der Wert unangetastet (Regel 34.21); lokal ist `retry` bereits 0. |
| E14 | P3 | **`isolate: false` halbiert die Suite-Laufzeit — gemessen, bewusst noch nicht übernommen.** Vier Läufe am 2026-09-12 (Node 22, jedes Mal 75 Dateien / 991 Tests grün): mit Isolation 26,77 s und 23,79 s, mit `--no-isolate` 13,18 s und 13,51 s — ≈45 % schneller; im Coverage-Modus 32,78 s → 16,35 s. Der Runner meldet denselben Befund selbst (75 Worker, ~104 ms Start je Datei, „at least ~7.72s faster with `isolate: false`“), die eigene Messung liegt deutlich darüber. Eine Abweichung ist dokumentiert: Branches gesamt 84,21 → **84,19** (0,02 Prozentpunkte), weil wiederverwendete Worker Modul-Initialisierung nur einmal zählen; alle Gates bleiben grün. | Die Entscheidung gehört in einen eigenen PR mit ADR, nicht stillschweigend in `vitest.config.ts`: `isolate: false` macht Modulzustand über Dateien hinweg sichtbar (Logger-Sinks, feste Uhren, Singletons). Vor der Übernahme dreimal grün in Folge plus gezielter Nachweis, dass kein Test auf frischem Modulzustand beruht; Determinismus schlägt Laufzeit (AGENTS 31). Bleibt der Nachweis aus, bleibt die Isolation — dann ist der Laufzeitgewinn verworfen, nicht aufgeschoben. |

| E16 | P3 | **Die Gates sind nachgezogen, die dünnsten Puffer sind benannt (Stand 2026-09-14).** Aus dem alten Fünf-Dateien-Befund sind alle fünf abgearbeitet: `elm327/protocol.ts` 96,3/76,0 → **100/100**, `elm327/stream.ts` 88,2/100 → **100/100**, `transport/can/bus.ts` 87,5/100 → **100/100** (eigene Spec mit fünf Registry-Fällen), `reports/report.ts` 100/79,5 → 100/**83,3**, `ai/heuristic.ts` 94,9/79,5 → **100/89,4**. Gates angehoben (ADR 0028): `core` 85/65 → **88/80**, `adapters` 85/75 → **92/78**, `transport` 85/70 → **88/72**, `reports` 95/75 → 95/**80**, `ai` 90/75 → **95/85**; dass sie beißen, ist mit je einer absichtlich unmöglichen Schwelle gemessen (`ERROR: … does not meet "packages/reports/**/src/**" threshold (99%) for …/pdf.ts`, `… for …/report.ts`). Der Ratchet erzeugt neue dünste Stellen, die hier stehen statt versteckt zu werden: `diagnostics/engine.ts` 82,10 Zweige (Puffer 2,1), `dtc/clear.ts` 90,24 Zeilen (2,2), `host/selection.ts` 95,40 Zeilen / 83,78 Zweige (3,4/5,8), `iso-tp/connection.ts` 92,85/75,62 (4,9/3,6), `ai/http.ts` 87,71 Zweige (2,7), `apps/web/src/adapters.ts` und `server.ts` (siehe E17). | Wie bisher: dünste Stelle nachtesten, dann in Schritten anheben (ADR 0017: erst Tests, dann Gate). Die Liste oben ist die Reihenfolge nach Puffer, beginnend mit `engine.ts`/`clear.ts` (core) und `http.ts` (ai); `connection.ts` braucht einen Timeout-/Overflow-Test doubles, nicht mehr Sleeps. Keine Ausnahme, kein `?? 0`-Arm, der als "nicht testbar" ausgenommen wird. |
| E17 | P2→✅ | **Die beiden dünnen Dateien sind nachgemessen und nachgetestet (2026-09-16).** Was die Zeile benannte: `server.ts` 69,63/65,53 und `adapters.ts` 73,68/**54,54** Zweige, je knapp über der Schwelle. Danach: `server.ts` **76,99 / 77,28**, `adapters.ts` **100 / 100**, `backend.ts` 92,81/67,87 (vorher 89,61/73,22 — die Szenario- und Panel-Pfade zählen jetzt mit). Getan: `apps/web/test/server-paths.spec.ts` (Freeze-Frame-Lesung bis aufs Rohbyte, jede Absage, Body-Limit, Marker), Adapter-Pins für die drei `create()`-Verweigerungen und die Katalog-Schalter, und `summarizeAvailability` ist gestrichen (Export ohne Aufrufer; die zwei Zweige, die die Datei drückten, waren ungenutzter Code). Schwelle auf 75/66, Biss gemessen. **Was bleibt, ist benannt statt gemeutert:** der CLI-Block am Fuß von `server.ts` (703-793: `--demo`, `--list-adapters`, Exit-Codes) bleibt in der Zahl ungeprüft, weil ein Kindprozess nicht in die v8-Deckung des Vaters zählt — ihn zu spawnen bringt Verhalten, keine Coverage, und die Zahl bleibt ehrlich niedrig; `backend.ts` steht bei 67,87 Zweigen, weil Export- und Session-Zweige (u. a. 1177, 1326-1332) denselben Nachlauf verdienen. | CLI-Block: ein Spawn-Test auf Verhalten (Exit 2 bei kaputtem `--adapter`, `--list-adapters` nennt Verfügbarkeit), mit dem Satz in 0.A, dass er die Zahl nicht hebt; **Nachtrag am selben Tag, zweiter Schnitt:** `apps/web/test/backend-paths.spec.ts` (8 Tests, Integration) geht die Zweige durch, die kein Happy Path berührt — die drei Replay-Quellen (gespeicherte Session, Inline-JSON, Pfad, den es nicht gibt), die Absage einer Session ohne Roh-Trace, die Managed-Absage eines Katalogeintrags unter fremder Id, der werfende Event-Listener samt Abbestellverschluss, das Marker vor jeder Verbindung, die Statistik-/Spektrum-Arme der Signalanalyse und die drei Chaos-Schalter mit Reset. `backend.ts` damit 92,81/67,87 → **95,56 / 73,89**, `server.ts` 76,99/77,28 → **77,12 / 77,42** (der `statusFor`-Zweig aus E23 zählt dort mit), Bodenschwelle `apps/web/src/**` 75/66 → **76/72** (ADR 0017: erst Tests, dann Gate). Biss gemessen: mit `branches: 74` fällt der Lauf mit `ERROR: Coverage for branches (73.89%) does not meet "apps/web/src/**" threshold (74%) for apps/web/src/backend.ts`. Dasselbe Nachlesen fand den zweiten toten Export und strich ihn: `get canBus()` — Dokstring „exposed for tests and embedding“, Aufrufer im ganzen Baum: keiner, die Replay-Specs spritzen ihren Bus über `BackendOptions.bus` ein; `backend.ts` 1427 → **1419** Zeilen, Größen-Ausnahme in `hygiene.test.ts` auf den neuen Wert nachgezogen (der Gate schlägt bei einer veralteten Zahl absichtlich). **Was übrig bleibt, ist benannt statt weggemessen:** die drei Aufräum-Fänge in `stop()` (595/599/603) brauchen ein Objekt, das beim Abmelden wirft; die Geführte-Diagnose-Zuordnung (771/776) einen laufenden Guide; die Anomalie-Zuordnung (1022) eine Stichprobe mit Ausreißer; die Actions-Zeile des Exports (1318) eine Sitzung mit Schreibaktion; 1169 den abgelehnten Marker; 427 ein Domänen-Ereignis, das nur bei echtem Timeout läuft — und der CLI-Block 707-797 bleibt, weil ein Kindprozess nicht in die v8-Deckung des Vaters zählt. | CLI-Block: ein Spawn-Test auf Verhalten (Exit 2 bei kaputtem `--adapter`, `--list-adapters` nennt Verfügbarkeit), mit dem Satz in 0.A, dass er die Zahl nicht hebt; die Restarme brauchen Injektion (Abbau, Guide, Anomalie), nicht noch einen Sleep. |
| E15 | P3 | **Der erste Schnitt ist gesetzt (2026-09-14), der zweite folgte beim Merge:** die Präsentation verlässt `backend.ts` — `apps/web/src/ecu-view.ts` (95 Zeilen: `EcuView`, `FreezeFrameView`, `toEcuView`, `toFreezeFrameView`), `apps/web/src/dtc-view.ts` (86: `DtcView`, `toDtcView(info, ecus)` statt `this.toDtcView`), `apps/web/src/trace-view.ts` (105: Sample/Marker/Trace plus die zwei Formatierer `formatValue`/`formatCanId`, jetzt ein Ort statt zwei — die Extraktion hatte eine Kopie von `formatCanId` erzeugt, die ein Test nicht fand). `backend.ts` 1338 → **1107** Zeilen, `analysis-input.ts` (114) kam in Runde 1.14 dazu; die `*View`-Typen bleiben über Re-Export aus `backend.js` erreichbar, `server.ts` und die Tests importieren unverändert (ADR 0014: outward API gleich). Dass das Messen dabei zwingt, ist gemessen: direkt nach dem Schnitt fiel `ecu-view.ts` mit **57,14/25** durch die neue Bodenschwelle (der Freeze-Frame-Mapper hatte keinen eigenen Test) — ein Test dazu (14 Tests in `apps/web/test/views.spec.ts`), und die drei Module stehen auf 100/100; `apps/web/src` als Ganzes 80,63/74,41 → 81,06/76,04. **Zweiter Schnitt (2026-09-14, beim Zusammenführen mit der Strictness-Linie):** `packages/runtime/src/services.ts` überschritt durch beide Linien zusammen das 800-Zeilen-Budget (809) — ausgelagert wurde `packages/runtime/src/sample-stream.ts` (96 Zeilen: `SampleRound`/`SampleListener` und die Abo-Logik `bind`/`unbind`/`subscribe`), `services.ts` 809 → **785** Zeilen, 5 neue Tests, das Modul steht auf 100/100/100/100. | Offen bleibt der Rest der Zeile: Präsentationszustand (`ecus`/`dtcs`/`resolution` als Backend-Felder) und die Frage, ob die HTTP-Schicht ihren Zustand an einen Kollaborateur übergibt; Adapter-/Bus-Auswahl liegt bereits in `adapters.ts` (138 Zeilen, 72/54,54 — siehe E17). Kein weiterer Schnitt, der Logik nach `public/app.js` verlagert (0.E-Regel: verschieben statt messen). |
| E22 | P3 | **Die Radkreis-Diagnose kann nur einen Kreis benennen.** Das Modell fährt vier Räder, verbiegt vier (`breakSensor()` an jeder Ecke) und seit 2026-09-16 melden sie auch alle vier über `0xF40D` — dokumentiert ist im Simulator-Paket aber genau ein Radkreis-Code (`C0035`, vorn links; `vehicle-monitors.ts` hat einen Monitor, `high-fidelity-package.ts` einen Code in `abs.dtcs`). Defekt an einem Hinterrad: kein Code, weil vorn links gegen die anderen drei plausibel bleibt (gemessen, in `high-fidelity-vehicle.spec.ts` festgehalten). Defekt an beiden Hinterrädern: `C0035`, also ein **Vorderrad**-Kreis, weil Regel 3 kein Erfinden undokumentierter Codes zulässt. Die Messung stimmt, der Name ist grob. | Die drei übrigen Kreise brauchen ihre J2012-Zuordnung **als Quelle**, nicht drei erfundene Nummern (AGENTS 13/23: ein Code ohne Beleg ist ein Gerücht mit Build). Mit Beleg: vier Monitor-Ids je Ecke (derselbe Rationalitätsvergleich, Bezug = die anderen drei), vier `dtcs`-Einträge im Paket, und der Attributionstest zieht mit. |
| E23 | ~~P3~~ | **geschlossen 2026-09-16.** Der eine Tipp zählt nicht mehr als Serverfehler: `@vdp/shared` meldet die Klasse (`UnknownEcuError`, `ErrorCode`-Mitglied `"E_ECU_UNKNOWN"`), `statusFor()` im HTTP-Layer ordnet zu — **409**, weil eine falsche Adresse Sache des Operators ist und nicht Defekt der Plattform (ADR 0018: eine Ablehnung ist eine Antwort mit Grund) — und kein Layer vergleicht Sätze. Der Satz selbst ist unverändert, weil er die Frage beantwortet („connect first or check the id“). Gepinnt zweimal: `packages/runtime/src/runtime.spec.ts` (Klasse, Code, `ecuId`) und `apps/web/test/server-paths.spec.ts` (Status 409 statt 500). Gemessen: `npx tsc --noEmit -p tsconfig.typecheck.json` ohne Befund, `npm run test:unit --project unit packages/runtime/src/runtime.spec.ts` 24 Tests, `npx vitest run --project integration apps/web/test` 156 Tests / 10 Dateien. | Die vier anderen Aufrufstellen von `unknownEcu()` teilen die Zuordnung, weil sie dieselbe Klasse werfen; nachgeprüft ist sie an einer Route, nicht an fünf — eine zweite Route dreht mit, wenn jemand die Klasse ändert, und das ist der Punkt der Klasse. |
| E24 | ~~P2~~ | **geschlossen 2026-09-16 — das Chaos sitzt jetzt im Pfad, und die Blende sagt, wohin ein Burst zielt.** Der Befund war doppelt: `injectChaos()` legte den `CanChaosBus` *neben* den Bus der Runtime (`backend.ts:1039-1041`, `openBus()` wickelte nie), und der Burst hing an einer hart codierten `0x7e0`. Gebaut: `start()` wickelt das Ergebnis von `openBus()` ein (eine Instanz, die Runtime, Roh-Trace und Schalter gemeinsam sehen — ADR 0014 §Konsequenzen), `injectChaos` ohne offene Verbindung ist eine Absage (`TransportClosedError` → **409**, keine ERROR-Zeile), `ChaosLab.injectBurstFrameDrop` nimmt jetzt `canId: undefined` als bus-weite Form (die `drop-count`-Regel zählt dann über alle Rahmen, `chaos-lab.spec.ts` pinnt das), und `chaosStatus()` meldet `dropBurstTarget` (formatiert) plus `dropBurstScope` (`none` / `bus-wide` / `targeted`) — das Panel hat dafür ein optionales Adressfeld (`#chaos-burst-can-id`) neben dem Feld, das die Korruptur schon hatte, und zwei stille Defaults (`?? 5`, `?? 0x7e8`) sind weg: die Felder prüfen jetzt die Grammatik der Route (`apps/web/src/route-input.ts`: `parseCanId`, `parseBurstCount`, `parseDropRate`). **Gemessen am laufenden Server auf dem verwalteten Fahrzeug** (`node apps/web/dist/src/server.js --port=8099`, built, Auswahl `simulator-5ecu`): bus-weiter Burst von 20 → `droppedFrames: 20`, verbleibend 0; auf `0x7f0` gezielt → nach 1,5 s noch 6 verbleibend *und* `dropBurstTarget: "0x7F0"` in der Antwort — die Stille ist ein Satz geworden; `dropRate: 1` friert die Probensammlung ein (135 → 135) und nach `reset` 281, vorher: 566 Rahmen gezählt, während die Session ungestört weiterlief. Im Test nachgeprüft wird die Wirkung an der Sitzung, nicht am Zähler: Probe-Einfrieren und Wiederaufnahme (`backend-paths.spec.ts`), und eine korrumpierte **Antwort**-Id nimmt 3 von 8 Codes aus dem Auslesebild (5 statt 8), nach Reset wieder 8. **Was bewusst nicht gebaut wurde:** `delayedFrames` hat weiterhin keinen Schalter (die Regel existiert im Labor, die Blende legt sie nicht), die Korruptur wirkt aufrahmengenau (Sequenznummern — Einzelrahmen sind davon nicht betroffen, gemessen: auf `0x7e0` gerichtet bleibt die Acht-Code-Lesung vollständig), und Chaos überlebt keine Reconnect, weil `stop()` die Regeln löscht. | Wer die Blende erweitert, prüft die Wirkung an einer Sitzung (ein Timeout, ein fehlender Code, eingefrorene Statistik) und nicht an einem Zähler: `assert.rejects` auf eine Zahl ist die alte Krankheit in neuem Test. |

| E21 | ~~P2~~ | **geschlossen 2026-09-16.** Der Runner ist ein Reiter, wie ADR 0040 §8 ihn will: eine Frage („Was passiert, wenn…?“), kein Meter. Katalog, Lauf, Ergebnis (Urteil, Checks, dekodierter Fehlerspeicher, Endzustand, Zeitlinie) hängen an `SimulatorControl.runScenario`, und die 70 Zeilen, die die 0.E-Regel nicht in `public/app.js` dulden wollte, sind zwei eigene Dateien geworden: `apps/web/public/scenario.js` (194 Zeilen) und die Projektion `apps/web/src/scenario-view.ts` (407 Zeilen, 100/93,18/100/100 Coverage, gemessen im Lauf von 1.35). `app.js` wuchs um Mount und Refresh auf 1648 Zeilen (Ausnahmegrund in `hygiene.test.ts` nachgezogen, ebenso `backend.ts` 1427). Der zweite Halbsatz der alten Zeile — „eine View, die niemand abdecken kann“ — ist nicht behauptet, sondern zugebaut: `apps/web/test/markup.spec.ts` pinnt die Selektoren *und* dass Panel-Hosts existieren, und die Projektion liegt im `integration`-Projekt. | Nur noch Feinheiten, die das Panel reicher machen, ohne neue Schichten zu erfinden: `run.timeline` trägt keine Live-Marken (die Zeitlinie ist deshalb Modellzeit), und `scenario.js` bleibt ohne eigene Rendering-Tests, solange es nur Zeilen des Payloads kopiert. |

| E20 | P3 | **Geschlossen 2026-09-24 — mit einer Ausnahme.** Die Gleichwertigkeit der Gates hing an Tests, nicht am Workflow. Seit heute trägt `ci.yml` einen eigenen **`quality`-Job** vor der Test-Matrix: `npm ci` → `build` → `typecheck:all` → `check` → `check:deps` → `check:manifests` → `npm audit`. Die Coverage-Gates laufen im Test-Job — **aber nicht als eigener Schritt**: `ci.yml` lädt `coverage/` mit `if: always()` hoch, und ein Artefakt, das auch kommt, wenn die Schwellen fielen, ist ein Bericht, kein Tor. Getragen werden sie von `tests/architecture/coverage-gate.test.ts` als Kindlauf innerhalb von `npm test` (nur `CI`, Rekursionssperre, `retry: 0`; gemessen +65 s pro Bein). Der Zweitträger in `guardrails.test.ts` — der Biome und beide `--noEmit`-Pässe ein zweites Mal in der Suite ausführte, weil der Workflow sie nicht trug — ist damit Doppelung und wurde **auf eine Selbstbeschreibung zurückgebaut**: er liest den Workflow-Text und fällt, wenn eines der sechs Tore seinen Träger verliert, und prüft, dass der `quality`-Job ein eigener bleibt (ADR 0059). Das sind ~2 s auf einem ~22 s-Bein, die die Suite jetzt für echtes Nachdenken hat. | Die eine Ausnahme ist der Schritt in E10: `npm run test:coverage` als eigener Workflow-Schritt, dann darf auch dieser Träger zurück auf die reine Selbstbeschreibung. Bis dahin bleibt er — weil der Upload das Tor nicht ersetzt. |

| E25 | P2 | **Der Rust-Kern (`crates/yes_you_can_core`, 661 Zeilen) ist ein Prototype neben dem Hauptsystem. Nachgemessen am 2026-09-24 — drei der fünf Befunde vom 2026-09-19 sind inzwischen im Code geschlossen, ohne dass der Eintrag nachgezogen wurde (Regel 34.24):** (1) ~~`isotp.rs` behauptet „SF up to 62 bytes (CAN-FD)“ und „4 GB with 32-bit DL“, implementiert ist Classic-CAN~~ **geschlossen** — der Modulkopf sagt heute „up to 7 bytes (Classic CAN)“ und „up to 4095 bytes (12-bit DL)“. (3) ~~`safety.rs::execute(current_time_ms, permit_expiry_ms)` nimmt das Permit-Ablaufdatum vom Aufrufer entgegen~~ **geschlossen** — Signatur ist `execute(self, current_time_ms: u64)`, geprüft wird gegen `permit.expires_at_epoch_ms`, `test_expired_permit_rejected` pinnt es. (2) ~~„zero-copy“/„zero-allocation“ ist unbelegt~~ **geschlossen als Claim** (2026-09-24, diese Runde) — `signal.rs` alloziert (`values.to_vec()`, zweimal `vec![0.0; n]`), `Cargo.toml`, `lib.rs` und der `signal.rs`-Kopf sagen das jetzt; `isotp.rs` sagt „borrows a slice … not a benchmark“ statt „Zero-allocation“. Ein Benchmark fehrt weiter — er ist nicht nötig, solange niemand die Behauptung braucht. **Offen bleibt (4):** `signal.rs` hat weiterhin **keinen** Test (`isotp.rs` 2, `safety.rs` 3). **Und (5):** der Crate hängt an keinem Tor — kein `cargo`-Schritt in CI oder `package.json`, keine Vektor-Kopplung an `tools/formal-conformance/vectors/`; TypeScript und Haskell grade gegen dieselben 72 Vektoren, Rust an keinens. **Zwei neue Befunde aus dem Lesen am 2026-09-24, ohne Toolchain nicht behebbar (beide an Datei und Zeile gebunden, siehe `crates/yes_you_can_core/README.md`):** (a) `src/isotp.rs:57` — `parse_frame` **panikt** bei einer Single Frame mit `SF_DL = 0`: der Zweig prüft `len > data.len() - 1` und schneidet dann `&data[1..=len]`; für `len == 0` ist das ein Bereich, dessen Anfang hinter seinem Ende liegt → `panic!` statt `Err(IsoTpError)`. Ein Dekodierer, der auf unvollständiger Eingabe panikt, gehört nicht an einen Bus. (b) `compute_fft` setzt `values.len() >= 4` voraus, prüft aber nicht, dass `timestamps` dieselbe Länge hat — ein kürzeres `timestamps` ergibt stillschweigend eine andere Abtastrate statt eines Fehlers. | Zwei Wege, beide vorab entschieden (Auftraggeber 2026-09-19): **Integration nur bei messbarem Hotspot** (Benchmark → nachweisen → WASM/native-Binding), sonst bleibt der Crate **Reference/Experimental**. **Neu seit 2026-09-24: der Status ist ein Tor statt eine Beschreibung.** `tests/architecture/reference-crate.test.ts` hält vier Tatsachen fest, von denen jede beißt: kein Produktions-Quelltext importiert `crates/`, kein `package.json`-Skript und kein Workflow ruft `cargo`, der Crate macht keine ungemessene Performance-Claim (Scan über `.rs`/`.toml`; eine Zeile, die sich selbst negiert, ist eine Korrektur und kein Claim — sonst bestrafte das Gate die Korrektur), und das README sagt **nicht gebaut / nicht getestet / nicht importiert**. Biss nachgemessen: ein wieder eingesetztes „High-Performance“ im `lib.rs`-Kopf lässt genau diesen Test fallen. Was bleibt, sind die zwei Lesen-Befunde (a)/(b) und (4): beides braucht `cargo`. Ein `#[cfg(test)]`-Block, den niemand übersetzt, ist ein Test, der nicht läuft — die Krankheit, gegen die „NOT RUN ≠ bestanden“ steht. 
| E26 | P3 | **`connection.ts` (845 Zeilen) steht über dem 800-Zeilen-Budget (gemessen 2026-09-23).** Der N_Cr-Produktionstimer (1.45) hat 60 Zeilen hinzugefügt (785 → 845): das `crTimer`-Feld, `armCrTimeout()`/`clearCrTimeout()` mit dem Timeout-Callback, die Arm-/Disarm-Stellen in den FF-/CF-/SF-Zweigen, Completion, Sequence-Error und `close()`, plus Docblocks. Die Datei enthält die gesamte ISO-TP-Verbindung (Sender + Receiver + Stats + Retry) als eine Klasse — die natürliche Review-Einheit, das Budget ist aber ein Budget und keine Grenze. | Split-Kandidat: die Timeout-Maschinerie als kleiner Kollaborator (rx-state-timer), der perspektivisch auch die N_Bs-Seite aufnehmen kann; danach den Ausnahmeeintrag aus `hygiene.test.ts` löschen — das Gate re-misst die Zahl selbst und fällt, wenn sie veraltet. |

| E28 | P3 | **Die Chronik der Spielregeln war zu einem Viertel doppelt (gemessen 2026-09-24 beim Auslagern nach ADR 0059).** Die 61 Bullets von `AGENTS.md` trugen nur **46 verschiedene Versionen**: 1.0–1.12 und 1.38 standen je zweimal darin. 13 der 14 Doppelungen sind **byte-identisch**, eine nicht — 1.38 hat zwei *verschiedene* Einträge (88 und 2.502 Zeichen) unter derselben Nummer; beides sind eigene Aussagen, also bleiben beide stehen. Entfernt wurden die 13 wörtlichen Doppel, kein Text umformuliert (Eintrag 1.48 der Chronik). Kein Gate hat das bemerkt, weil die Datei 45 KB groß war und niemand sie rückwärts las — ein Protokoll, in dem dieselbe Zeile zweimal steht, ist ein Protokoll, das keiner mehr liest. | Ein Tor dafür, analog `links.test.ts`: jede Versionsnummer der Chronik kommt höchstens einmal vor, und zwei Bullets derselben Nummer müssen sich in den ersten 80 Zeichen unterscheiden (sonst ist es die Doppelung, die es zu melden gilt). Bis dahin ist der Befund hier festgehalten; die 1.38-Kollision bleibt als eigene Frage offen, welche der beiden Nummern stimmt. |
| E30 | P2 | **Ein transienter Bus-Fehler tötete die ganze Anfrage, statt einen Retry zu kaufen.** Nachdem `send()` ELM-Fehler wirft (E29 Punkt 3), erkannte `isRetryable()` in `packages/transport/iso-tp/src/connection.ts` nur noch `IsoTpError` mit `timeout: N_Bs` oder `N_Cr`. Ein `BUS BUSY` oder `CAN ERROR` — auf Bluetooth SPP mit 50–150 ms Funkverzögerung und Jitter routine, nicht Ausnahme — ging also als nicht-retrybar durch und beendete die Anfrage. `SLOW_LINK_TIMING.maxRetries: 2` aus E29 war für genau diese Fehlerklasse nie wirksam. | **Behoben, in drei Teilen, weil der Fehler an einer Naht lag.** (1) `protocol.ts` führt `ELM_TRANSIENT_ERRORS` und `isTransientElmError()`: transient sind `NO DATA`, `BUFFER FULL`, `BUS BUSY`, `BUS ERROR`, `CAN ERROR`, `UNABLE TO CONNECT`, `FB ERROR`, `STOPPED`; permanent sind `DATA ERROR`, `<DATA ERROR`, `ERR`, `?` — wer die gleichen Bytes noch mal schickt, bekommt dieselbe Ablehnung und verbrennt nur das Timeout zweimal. (2) `adapter.ts` gibt `retryable: isTransientElmError(error)` in die `TransportError`-Details. (3) `isRetryable()` akzeptiert eine `TransportError` mit `details.retryable === true`, **und** `transmit()` verbreitet `error.details`, statt sie mit `cause: messageOf(error)` in einen String zu platten — ohne diesen dritten Teil starb das Flag auf dem Weg nach draußen und die ersten beiden Teile waren wirkungslos. Die Aufteilung bleibt Absicht: der Adapter *klassifiziert*, ISO-TP *entscheidet*. Drei Biss-Nachweise: `transmit()`-Verbreitung entfernt → rot; `isRetryable` auf `return false` gezwungen → rot; `"BUS BUSY"` aus der Menge genommen → rot. | **Was dieser Eintrag nicht beweist:** dass der Adapter „komplett sicher funktioniert‘. Der Fund ist der Gegenbeweis zur grünen Suite als Sicherheitsversprechen — 78 grüne Tests in `iso-tp` + `elm327` haben diese Lücke nicht gesehen. Ohne Hardware bleiben die beiden offenen Punkte aus E29 offen (PIC18F25K80-Halbduplex, Bluetooth-Reconnect), und jeder Satz über das Gerät bleibt ein Satz über den Code. |
| E29 | P2 | **Der ELM327-Pfad war auf vier Ebenen gegen einen Bluetooth-Adapter gerichtet (gemessen 2026-09-24, alle Befunde am Code, nicht an Hardware).** (1) **Die Init-Sequenz widersprach dem Parser.** `DEFAULT_INIT_SEQUENCE` schickte `ATS0` (Leerzeichen aus), während `parseFrameLine` auf Leerzeichen splittet und ein Ein-Token-Dokument verwirft — mit `ATS0` würde **jeder** empfangene Frame verworfen. (2) **`ATCAF0` fehlte**, obwohl der Katalog den Adapter seit immer als „raw CAN mode (ATH1/ATCAF0)“ beschreibt: mit `CAF1` baut die Adapter-Firmware eigene Flow-Control-Frames und beantwortet die des TypeScript-Stacks (AGENTS 5, `isoTpOffload: false`). (3) **`send()` schluckte ELM-Fehler.** `CAN ERROR`, `BUFFER FULL`, `STOPPED` landeten nur in `status.errors`; ISO-TP hielt den Frame für gesendet und wartete auf eine Antwort, die nie angefordert wurde — ein Timeout, der wie „das Steuergerät schweigt” liest statt „der Adapter hat verweigert“. (4) **`ATSP6` war eine Konstante**, obwohl `formatIdentifier` 29-Bit bereits kann: ein 29-Bit-Fahrzeug wurde nie gehört. Dazu zwei Windows-Befunde: `SerialStreamOptions.device` dokumentierte `COM3` als Beispiel, und `probeSerialDevice` beantwortete `fs.stat("COM3")` unter Windows für jeden existierenden Port ENOENT — dokumentiertes Win32-/`node:fs`-Verhalten, **nicht** hier gemessen, diese Arbeitsumgebung ist Linux) mit „is the adapter plugged in?“ — ein Hinweis, der einen Operator nach einem Kabel suchen lässt, das steckt. | **Behoben:** `ATS1` + `ATCAF0` in der Sequenz, `send()` wirft `TransportError` mit Befehl und Frame-ID, `canProtocol`/`--protocol`/`#adapter-protocol` reichen 6–9 von der UI bis zum Adapter durch (inklusive `selectionFromPayload` als Zahl, nie als Wort), Zeilen-Trennung auf `\r\n`, `\n` **und** bare `\r`, `ByteStream.onError?` als optionaler Vertragsteil, den der Adapter abonniert (ein Gerät, das weg ist, ist kein Adapter), `isWindowsComPortName`/`isWindowsBareComPort` mit injizierbarer Plattform, und `SLOW_LINK_TIMING` (nBs/nCr 2000, sendTimeout 3000, 2 Retries) als **benanntes Profil** — `DEFAULT_TIMING` bleibt 1000/0, weil ein Default, der lockerer wird, jedes Timeout-Gate leiser bestehen lässt. Jede der acht Änderungen hat einen Biss-Nachweis. | **Offen, weil es ohne Hardware nicht entscheidbar ist:** der Halbduplex-Konflikt des PIC18F25K80. Wenn der Adapter nach einem First Frame noch Daten ausgibt und den Prompt `>` noch nicht geschickt hat, bricht jedes an ihn gesendete Byte den Vorgang mit `STOPPED` ab — der Flow-Control-Frame, den ISO-TP sofort nach dem First Frame senden muss, kann also am Adapter selbst scheitern. `STOPPED` steht in `ELM_ERRORS` und wirft jetzt (Punkt 3), aber *wann* der Prompt kommt, ist eine Eigenschaft des Geräts, nicht dieses Codes. Ein Reconnect bei Bluetooth-Abbruch ist ebenfalls nur die Hälfte: der Host-Stream meldet den Verlust jetzt (Punkt 6), der Adapter öffnet aber nicht von selbst wieder. **Widerlegt und nicht geändert:** die Behauptung, das DLC-Byte in `formatSendPayload` sei ein Konzeptionsfehler. Es ist dasselbe Feld, das `parseFrameLine` zurückliest; ohne es verschiebt sich jede Nutzlast um ein Byte in beide Richtungen. Die Begründung steht jetzt als Kommentar an der Funktion, damit die nächste Runde nicht von vorn diskutieren muss. |
| E27 | P2 | **Die Ernte schreibt die Beobachtung, aber drei Stellen lesen sie noch nicht (gemessen 2026-09-23 am Stand von ADR 0058).** (1) **`verify.ts` misst 72,34 Zeilen / 58,97 Zweige** — die niedrigste Datei des Baums unter den neuen: der `odxtools`-Pfad läuft nur, wenn die Bibliothek installiert ist (`VDP_ODX_PYTHON`), und in dieser Umgebung ist sie es außerhalb des venv nicht. Die fünf Tests der Gegenprüfung zählen deshalb zu den 8 Skips von `npm test`; wer sie grün sehen will, braucht `pip install odxtools`. Das ist die Ehrlichkeitsregel (`NOT RUN` ≠ bestanden), aber sie heißt auch: der Encode-/Decode-Rundlauf ist **einmal manuell gemessen** (29/29, 0 Abweichungen, odxtools 11.6.0), nicht in jedem CI-Lauf. (2) **`cli.ts` misst 78,13 Zeilen / 53,57 Funktionen** — der Adapter-Zweig (`openAdapterBus`) ist ohne Hardware nicht fahrbar: getestet ist die Absage (Exit 3, `--device /dev/does-not-exist-vdp`), nicht der Erfolg. Derselbe Befund wie bei `server.ts`' CLI-Block (E17) und beim `flaky-reporter` (E10): ein Kindprozess/Device zählt nicht in die Deckung des Vaters. (3) **`fault-memory.ts` misst 85,93/66,66** — die Zweige, die fehlen, sind die NRC-Pfade der Record-Lesung (`0x19 0x04/0x06` mit einem anderen NRC als `requestOutOfRange`); der Simulator verweigert sie nicht auf die Arten, die ein reales Steuergerät tut. | (1) Einen CI-Träger für die Gegenprüfung, analog zum Haskell-Gate (ADR 0055): `pip install odxtools` in einem Job-Schritt und `VDP_ODX_PYTHON` gesetzt — braucht das Workflow-Recht aus E10, bis dahin bleibt der lokale Lauf der Beleg. (2) Den Adapter-Zweig gegen einen **Replay-Bus** fahren (`--trace`), statt auf Hardware zu warten: dieselbe CLI, ein aufgezeichneter Trace als Fahrzeug — das ist ADR 0005 auf die Ernte angewendet und fehlt heute (`--adapter replay` ist im Katalog, aber die Ernte hat keinen Trace-Pfad). (3) Die NRC-Pfade mit `FaultyLink` (ADR 0039) injizieren statt sie zu simulieren: ein Link, der `0x78`-Pending, Timeout und `conditionsNotCorrect` (0x22) liefert, deckt die Zweige, die ein kooperativer Simulator nie zeigt. |

---

| E31 | P2 | **Der Adapter-Audit für den Hardware-Tag fand Ladefehler in allen fünf Adaptern und in den Katalog-Nähten (gemessen 2026-09-24 am Stand des vorherigen Commits, Beweise: PTY-Geräte-Emulatoren über der echten Seriell-Strecke — 78 grüne Suite-Tests hatten keinen davon gesehen).** (1) **ELM327: jede Multi-Frame-Antwort starb an einem windowed Device als `ELM327 refused the frame: STOPPED`** — die Befehle waren nicht serialisiert (zwei in-flight Kommandos teilten sich `currentLines` und der zweite traf das Device mitten im Empfangsfenster); reproduziert über PTY genau so, wie E29 es als „ohne Hardware nicht entscheidbar“ offen gelassen hatte: Varianten-Emulator „Fenster + STOPPED bei Eingabe“. (2) **CANable meldete `connected` auf einem stillen Kabel**: `open()` schrieb S6/Z1/O und wartete nirgends die Lawicel-Antwort ab (CR/BEL) — eine verworfene Bitrate oder ein gar nicht vorhandenes Gerät las sich wie ein schweigendes Fahrzeug. (3) **CANable-`onError` war nicht verdrahtet** — der vom ELM327-Fix (E29) eingeführte Stream-Vertragsteil. (4) **Listen-only widersprach sich selbst:** der Katalog schrieb `L\r` beim Erstellen und `open()` sendete normalerweise `O` — Lawicel liest `O` als Normalmodus, die Reihenfolge entschied gegen die Absicht. (5) **`commandTimeoutMs` war dokumentiert, aber ungenutzt** (Kommentar „is not read yet“). (6) **SocketCAN: der Lader akzeptierte nur die `open()`-Form** — das per Hinweis empfohlene `npm i socketcan` bringt `createChannel`, der Hinweis zeigte also in eine Sackgasse. (7) Keine Bindung → generische „not installed“ ohne Interface-Wahrheit: `/sys/class/net` liefert Existenz/ARPHRD_CAN/operstate ohne etwas zu öffnen — „Interface down” als häufigster realer Fehler stand nirgends als Ursache. (8) **`generic-can` zählte rx pro Subscriber** statt pro Bus-Frame. (9) **Baugrößen-/Hygiene-Reste:** der `--doctor` aus demselben PR hätte `server.ts` über das 800-Zeilen-Budget gehievt, der can-utils-Fallback enthielt ein leeres `catch {}` und die Rehearsal einen eigenen Sleep statt `tests/helpers/wait.ts`-`tick()` (Gates greifen: hygiene/coverage). | **Behoben, alles mit PTY-/Unit-Regression:** (1) `Elm327Adapter` serialisiert Kommandos (Queue; Close/Stream-Fehler lassen Queued schnell scheitern; Timeout steht in `status.errors`) — danach: beide Prompt-Timings über die volle ISO-TP-Multi-Frame-Kette grün, gemessen in `tests/integration/adapter-rehearsal.spec.ts` (4× ELM + 2× slcan Szenarien). (2)+(5) CANable `open()` beweist das Gerät: `V`-Handshake, Bitrate-/`O`/`L`-Acks mit Timeout/BEL-Deutung und Fehlermeldung mit Port/Baud/Hinweisen, `Z1`-Ablehnung tolerant (USBtin), fehlgeschlagenes open() setzt sauber zurück (kein halb-offener Zustand). (3) `onStreamError` suspendiert `isOpen()` und reißt die offene Config mit Grund mit. (4) `CanableOptions.listenOnly` — `open()` sendet `L` **statt** `O`; der Katalog schreibt nichts mehr selbst (side-effect-frei pinnt `host.spec.ts`). (6) `tryLoadSocketCanBinding` akzeptiert beide Formen und wrappt `createChannel` in den Vertrag (RTR verworfen wie slcan-remote). (7) `host/socketcan-fallback.ts`: `/sys`-Wahrheit (`canInterfaceState`, `listCanInterfaces` ohne Root, ohne Öffnen), Fallback-Binding über **can-utils** (candump/cansend, ein Prozess pro Frame — benamt), Auflösungskette native → can-utils → „beides fehlt, hier die zwei Befehle“, Probe botet /sys-Wahrheit mit Bindlist als Aufräum-Antwort. (8) Eine wrapped Subscription, lokale Filter — rx zählt Bus-Frames. Zusatz **Adapter-Doctor** (`host/src/doctor.ts`, CLI `--doctor` in `apps/web/src/doctor-cli.ts`, npm-Script `adapter:doctor`, Exit 0/2/3): Einstellungen → Verfügbarkeit → Öffnen/Handshake(Jeweils Firmware: ELM-Version/ATDP, slcan `V`) → Fahrzeugspannung (`ATRV`) → funktionaler TesterPresent-Ping (read-only, ISO 14229-1 §9.4, unterdrückte positive Antwort bewusst aus) — der erste ✗ nennt Ursache + Hinweise; E2E über PTY (scripted ELM + scripted slcan) gemessen: ELM327 exit 0 mit „Firmware: ELM327 v2.1 · 13.8 V · Antwort: 0x7E8”, slcan exit 0 mit „Firmware: 0101 · Antwort: 0x7E8”. Dokumentation: **docs/adapter-checkliste.md** als Tages-Ablauf. | **Ehrlich offen:** Echte PIC18-Klonen-Timings abseits der beiden gemessenen Varianten bleiben Geräts-Detail (letzter Beweis ist das Auto); der can-utils-Fallback kostet ~1–5 ms/Frame (ein Prozess pro `cansend` — Diagnose ok, Busflut nicht); Bluetooth-Reconnect weiterhin nur „Verlust wird klar gemeldet“, kein Auto-Reopen; Jar Web-Panel zeigt den Doctor nicht (CLI ist die Fläche). Was dieser Eintrag nicht beweist: dass Adapter „am Fahrzeug” funktionieren — Tag X bleibt Teil der Messung. |

---

| Nr. | P | Befund (gemessen 2026-09-24, am Code — Beweise: Unit-Regressionen; das PTY-Rehearsal blieb unangetastet grün) | Verbesserung (Bestehendes verbessern, kein Feature) |
|---|---|---|---|
| E32 | P2 | **Der zweite Adapter-Audit (CAN-Protokoll-Seite) fand vier Tragflächen, die am Hardware-Tag zuerst brechen.** (1) **SocketCAN konnte CAN-FD nicht tragen:** `SocketCanFrameData` hatte kein `fd`/`brs`, `handleFrame` hardcodierte `fd: false` und `send()` warf das Flag weg — ein FD-fähiger USB-Adapter (GSUSB, PCAN-USB FD), mit `canFd: true` gewählt, hätte ISO-TP-64-Byte-Segmente als klassische Frames auf die Leitung geschickt (die der Kernel verweigert) und jedes empfangene FD-Frame wäre als klassisch deklariert worden; die Rahmen-Ebene führt CAN-FD-DLC-Tabellen (`dlcToLength`/`lengthToDlc`), die auf dieser Bahn nie ankamen. (2) **Der can-utils-Fallback warf FD-Zeilen weg** (`ID##…` → `null`, dokumentiert „classic-CAN-only“) — dasselbe auf dem Rückweg. (3) **ISO-TP nahm jede deklarierte Länge an:** die FD-Fluchtform von FF_DL ist ein 32-Bit-Feld (ISO 15765-2 §9.5.2), `expectedLength` wurde ungeprüft übernommen — ein korruptes oder feindliches First Frame alloziert `chunks` für bis zu 4 GiB, und einzig der N_Cr-Timer merkt es eine Sekunde später; die Norm-Antwort des *Empfängers* auf „länger als mein Puffer“ — Flow Control mit flowStatus **Overflow** (Table 14) — existierte im Code nicht (nur die Sender-Seite kannte `FLOW_STATUS.OVERFLOW`). (4) **Vier lokale Kopien derselben Filter-Logik** (elm327, canable, socketcan, generic-can) ignorierten das `extended`-Flag eines Filters: `0x18DA10F1 & 0x7FF === 0x0F1` — ein Subscriber, der den 11-Bit-Filter `0x0F1` setzte, bekam auch das 29-Bit-Frame; die geteilte Funktion `frameMatchesFilters` (die `extended` prüft) existierte bereits und lief nur im Replay. Dazu zwei CI-/CLI-Befunde: (5) **Die nächtliche Hardware-CI maß nichts:** `hardware.yml` legte vcan0 an, installierte aber weder das native Modul noch can-utils → `loadSocketCanBinding()` = `null` → die Conformance-Suite skippte jeden Lauf, der Job testete nur den Simulator. (6) **Die Ernte kannte `--protocol` nicht** (`ADAPTER_FLAGS` ohne den Eintrag): ein 29-Bit-Fahrzeug war am Ernte-Tag über die Ernte-CLI nicht erreichbar — dasselbe Flag, das E29 für die Workbench geradegezogen hatte. | **Behoben am 2026-09-24, jede Änderung mit Biss-Nachweis:** (1) `SocketCanFrameData.fd/brs` als Vertragsteil, Adapter-Passthrough in beide Richtungen, npm-`createChannel`-Wrapper übergibt die Flags duck-typed (ein Modul, das FD nicht kennt, ignoriert sie); `--can-fd` reist durch die ganze Kette — `parseAdapterArgv` (Boolean, Fail-closed mit Wert), `selectionFromPayload` (nur `=== true`), Katalog-`create` (Capability nur auf Wunsch — verhandelt, nie vermutet, AGENTS 4), Hilfe und `describeAdapterConfig`. (2) can-utils: `##`-Form in beide Richtungen (Format gegen can-utils `lib.c` verifiziert: `parse_canframe` nimmt `<id>##<flags><data>` mit optionalen Punkten, `snprintf_canframe` mit `sep=0` druckt ohne; BRS = Bit 0, ESI = Bit 1 nach `linux/can.h`); `parseCandumpLine` akzeptiert beide Schreibweisen der Flags-Trennung, mehr als 64 Byte Payload bleibt eine Absage. (3) `maxReceiveBytes` (Default `DEFAULT_MAX_RECEIVE_BYTES` = 64 KiB — die größte Standard-Antwort ist der 4-KiB-DID-Lesung): First Frame darüber → **FC Overflow + `failPending` mit Grund** statt stummer Rezeption; klassisches CAN unverändert (12-Bit-FF_DL ≤ 4095 liegt unter der Grenze, die Fluchtform wird dort weiterhin verworfen). (4) Alle vier Adapter rufen `frameMatchesFilters` — ein Vokabular, vier Aufrufstellen, die Kopien sind weg (Regel 34.2). (5) `tests/hardware/socketcan-conformance.test.ts` nutzt die Host-Auflösungskette (nativ → can-utils) statt nur des npm-Moduls; `hardware.yml` installiert can-utils (`apt-get install -y`, vorgebaut, kein node-gyp). (6) Ernte-CLI kennt `--protocol` und `--can-fd` als Adapter-Flags. **Messung:** Suite 2629 → **2642 Tests** grün (+13: FD-Rundläufe beider Bindungen, `##`-Roundtrip, Overflow/Continue-FC mit Byte-Beweis, extended-Filter-Pins je Adapter, `--can-fd`-Kette); Coverage-Gates (adapters 92/78, transport 88/72) unverändert scharf, die berührten Dateien gemessen: `iso-tp/connection.ts` 92,85/75,62 → **94,57/87,37**, `socketcan/adapter.ts` → **100/100**, `host/socketcan-fallback.ts` **97,12/83,96**; das Hygiene-Gate griff beim Zeilenbudget (`connection.ts` 861 → 888 nachgemessen und in der Ausnahme begründet — genau dafür ist das Tor da). **Ehrlich offen:** FD am echten Fahrzeug bleibt eine Tag-X-Messung (die Syntax ist gegen can-utils `lib.c` verifiziert, nicht gegen einen GSUSB); die Workflow-Änderung an `hardware.yml` braucht das E10-Recht, falls die GitHub-App sie nicht schieben darf. |
| E33 | P3 | **Der Adapter-Doctor ist CLI-only** (E31, „ehrlich offen“): das Web-Panel zeigt Probing, aber nicht die Techniker-Checkliste — am Fahrzeug steht die Console daneben, nicht der Browser. | `POST /api/adapter/doctor` (Rückgabe: `AdapterDoctorReport` aus `@vdp/adapter-host`, inklusive Steps/Hinweise), ein Button im Adapter-Panel, der dieselbe Auswahl prüft. Der Doctor ist bereits bibliotheksfähig; es fehlt nur die Route und die Projektion in `apps/web`. |
| E34 | P3 | **Ein Verlust der Verbindung wird gemeldet, aber nie wieder aufgenommen** (E31/E29, „bewusst nicht automatisiert“): Bluetooth-ELM327 und USB-tty sterben reproduzierbar am P2-Rand (`onError` → `isOpen() false`, Fehler im Status) — dann ist die Sitzung von Hand neu zu starten, inklusive Live-Werte. | Eine begrenzte, benannte Reconnect-Policy am Host-Stream (z. B. ein Versuch nach `X` ms, nur wenn kein Request in flight, danach der klare Endzustand von heute): kein stiller Endlos-Loop, jede Wiederaufnahme ein Log-Eintrag mit Grund. Sicherheitsfrage zuerst (AGENTS 26): ein Reconnect ist ein Verbindungsaufbau, kein Write — die Permit-Kette bleibt unberührt. |
